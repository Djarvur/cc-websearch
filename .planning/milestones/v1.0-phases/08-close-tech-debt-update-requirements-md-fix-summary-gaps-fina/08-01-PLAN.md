---
phase: 08-close-tech-debt-update-requirements-md-fix-summary-gaps-fina
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/REQUIREMENTS.md]
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09]
user_setup: []

must_haves:
  truths:
    - "REQUIREMENTS.md reflects DDG-only architecture with no API key requirement"
    - "CI-01 through CI-08 are defined in REQUIREMENTS.md and mapped to Phase 6"
    - "FTEC-04 is moved to Out of Scope with reason documented"
    - "PLUG-02/03 reference actual .cjs paths with CLAUDE_PLUGIN_ROOT"
    - "SRCH-02 documents the `<snippet>` tag alongside `<title>` and `<url>`"
    - "Traceability table uses standardized statuses: Complete / In progress / Not started"
    - "All 10 SATISFIED requirements have [x] checkboxes"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement definitions, traceability, and checkboxes"
      contains: "CI-01 through CI-08 definitions, SRCH-04 rewritten as DDG-only, FTEC-04 in Out of Scope"
  key_links:
    - from: "REQUIREMENTS.md traceability table"
      to: "v1.0-MILESTONE-AUDIT.md requirements coverage table"
      via: "checkbox sync from milestone audit SATISFIED status"
---

<objective>
Update REQUIREMENTS.md to reflect the current DDG-only architecture, add CI requirements, fix stale descriptions, standardize traceability conventions, and sync all checkboxes.

Purpose: Close the tracking debt identified in the v1.0 milestone audit. REQUIREMENTS.md has not been updated since Phase 4 and contains stale references to Perplexity, missing CI definitions, and outdated checkbox state.

Output: Updated `.planning/REQUIREMENTS.md` with current architecture descriptions, CI requirements, standardized traceability, and synced checkboxes.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/08-close-tech-debt-update-requirements-md-fix-summary-gaps-fina/08-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite outdated requirement descriptions</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    @.planning/REQUIREMENTS.md — full file, all sections
    @.planning/v1.0-MILESTONE-AUDIT.md — requirements coverage table (lines 96-121), tech debt summary (lines 155-176)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` to implement four description rewrites:

    1. D-01: Rewrite **SRCH-04** entry. Replace the description `"Perplexity Chat Completions API as primary search provider — extracts results from response citations and content"` with `"DuckDuckGo Lite HTML is sole search provider — search results include citation URLs and `<snippet>` descriptions extracted from DDG result HTML"`.

    2. D-02: Move **FTEC-04** from its current position to the Out of Scope table. Remove the entry from the WebFetch section. Add a row to the Out of Scope table: `| LLM summarization via Perplexity (FTEC-04) | removed Phase 5 — Perplexity summarization no longer needed |`. Use the table's existing two-column format.

    3. D-04: Rewrite **CONF-01** entry. Replace `"API keys read from environment variables ('PPLX_API_KEY', etc.) as primary config source"` with `"No API keys required — DDG is sole provider, zero-config setup"`.

    4. D-05: Rewrite **CONF-03** entry. Replace the existing description with `"Config file supports: retry params (max retries, base delay, max delay), log level"`. Remove the phrase `"API keys, Perplexity model selection"` entirely. Only retry params and logging level remain as supported config fields.

    Preserve all other content exactly. Do not reformat or restructure the file beyond these specific changes. Maintain existing `[ ]`/`[x]` checkbox format and ID references.
  </action>
  <verify>
    <automated>grep -c 'Perplexity Chat Completions API as primary' .planning/REQUIREMENTS.md && echo "FAIL: SRCH-04 not updated" || echo "PASS: SRCH-04 updated"</automated>
    <automated>grep -c 'FTEC-04.*LLM summarization' .planning/REQUIREMENTS.md | grep -q 0 && echo "PASS: FTEC-04 removed from WebFetch" || echo "FAIL: FTEC-04 still in WebFetch"</automated>
    <automated>grep -c 'API keys read from' .planning/REQUIREMENTS.md | grep -q 0 && echo "PASS: CONF-01 updated" || echo "FAIL: CONF-01 not updated"</automated>
    <automated>grep -c 'Perplexity model selection' .planning/REQUIREMENTS.md | grep -q 0 && echo "PASS: CONF-03 stripped" || echo "FAIL: CONF-03 still has Perplexity"</automated>
  </verify>
  <acceptance_criteria>
    - SRCH-04 says "DuckDuckGo Lite HTML is sole search provider" (not "Perplexity")
    - FTEC-04 is not in the WebFetch section; it appears in the Out of Scope table
    - CONF-01 says "No API keys required — DDG is sole provider"
    - CONF-03 lists only "retry params" and "log level" — no API keys or Perplexity model selection
  </acceptance_criteria>
  <done>All four requirement descriptions rewritten. Grep confirms absence of old Perplexity-era language.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add CI requirements and update PLUG/SRCH documentation</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    @.planning/REQUIREMENTS.md — full file
    @.planning/v1.0-MILESTONE-AUDIT.md — CI requirements gap (line 124)
    @.planning/ROADMAP.md — Phase 6 requirements: CI-01 through CI-08
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` to add CI requirement definitions and update PLUG/SRCH documentation. Insert the CI section after the existing WebFetch section and before the Config and Infrastructure section. Use the same format and checkbox style as existing requirements:

    2a. **D-03: Add CI requirement definitions** — Create a new section header `### CI Pipeline` (same heading level as `### WebFetch`). Add these eight requirement definitions below it, each with `[ ]` checkbox prefix and bold ID:

    - **CI-01**: GitHub Actions PR gate workflow — runs on push/PR, installs deps, builds, runs lint, typecheck, unit tests with coverage
    - **CI-02**: E2E test suite validates real plugin behavior — bundled scripts produce correct XML output against live DDG and web
    - **CI-03**: Periodic cron workflow (weekly) runs npm audit and E2E tests against live services
    - **CI-04**: CI fails on test failures, type errors, lint violations, coverage threshold drops — no silent passes
    - **CI-05**: Local toolchain mirrors CI: ESLint flat config, Prettier formatting, mise task runner
    - **CI-06**: Coverage thresholds enforced (80% statements, 70% branches, 80% functions)
    - **CI-07**: mise task runner provides `mise run check-all` for local CI parity
    - **CI-08**: Dependabot configured for npm and GitHub Actions ecosystem with weekly updates

    2b. **D-07: Update PLUG-02/03 paths** — Edit both PLUG-02 and PLUG-03 descriptions. Replace any reference like `scripts/websearch.js` with `scripts/websearch.cjs`, and ensure the path is expressed as `node "${CLAUDE_PLUGIN_ROOT}/scripts/websearch.cjs"` using `Bash` tool. The current text likely says `"${CLAUDE_SKILL_DIR}/../scripts/websearch.js"` — update the extension from `.js` to `.cjs`.

    2c. **D-08: Update SRCH-02** — Edit the SRCH-02 description to include `<snippet>` tag. The text should read: `"Script outputs '<search_results>' XML format with '<result>', '<title>', '<url>', '<snippet>' tags matching Claude Code exactly"`.

    Add the CI section requirements to the traceability table at the bottom of the file, with Phase 6 and Status "Complete" for all CI-01 through CI-08 entries.

    Update the coverage count at the bottom: increment the v1 requirements total from 22 to 30 (22 original + 8 new CI requirements).
  </action>
  <verify>
    <automated>grep -c 'CI-01' .planning/REQUIREMENTS.md | grep -q 1 && echo "PASS: CI-01 defined" || echo "FAIL: CI-01 missing"</automated>
    <automated>grep -c 'CI-08' .planning/REQUIREMENTS.md | grep -q 1 && echo "PASS: CI-08 defined" || echo "FAIL: CI-08 missing"</automated>
    <automated>grep -c 'websearch.cjs' .planning/REQUIREMENTS.md | grep -q 1 && echo "PASS: .cjs path used" || echo "FAIL: .js path still present"</automated>
    <automated>grep -c '<snippet>' .planning/REQUIREMENTS.md | grep -q 1 && echo "PASS: SRCH-02 has snippet tag" || echo "FAIL: SRCH-02 missing snippet"</automated>
  </verify>
  <acceptance_criteria>
    - CI-01 through CI-08 defined as requirement entries with `[ ]` checkboxes
    - CI-01 through CI-08 appear in the traceability table with Phase 6, Status "Complete"
    - PLUG-02 and PLUG-03 reference `.cjs` files, not `.js`
    - SRCH-02 includes `<snippet>` in its tag list
    - v1 requirements total updated from 22 to 30
  </acceptance_criteria>
  <done>CI requirements defined, PLUG/SRCH docs updated, traceability table extended.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Standardize traceability statuses and sync checkboxes</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    @.planning/REQUIREMENTS.md — traceability table section
    @.planning/v1.0-MILESTONE-AUDIT.md — requirements coverage table (lines 96-121), specifically the "Final Status" column
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` to standardize the traceability table and sync checkboxes.

    3a. **D-06: Standardize statuses** — In the traceability table (`## Traceability`), convert any `Status` values to exactly one of these three: `Complete`, `In progress`, or `Not started`. Currently the table uses a mix of "Pending", "Done", "Complete" — normalize all to `Complete` where they are proven SATISFIED, and keep other values as appropriate:
    - "Pending" -> "Not started" (for items not yet satisfied)
    - "Done" -> "Complete"
    - Keep "Complete" as-is
    - If any requirement is fully satisfied, it should be "Complete"

    Based on the milestone audit (all 22 v1 requirements are SATISFIED or PARTIAL), all entries should be `Complete` except FTEC-04 (should be removed or listed with appropriate note).

    3b. **D-09: Sync checkboxes** — Using the v1.0-MILESTONE-AUDIT.md requirements coverage table (lines 96-121) as the authoritative source, update all 10 checkboxes that show `[ ]` to `[x]`. The requirements with SATISFIED status that need checkbox updates are:
    - PLUG-01, PLUG-02, PLUG-03, PLUG-04, PLUG-05 (all Phase 1, all SATISFIED)
    - SRCH-01 (Phase 1+5, SATISFIED)
    - SRCH-02 (Phase 1, SATISFIED)
    - SRCH-04 (Phase 1+5, SATISFIED — reinterpreted as DDG citations)
    - CONF-01 (Phase 1, SATISFIED)
    - CONF-04 (Phase 1, SATISFIED)

    For SRCH-03, SRCH-05, SRCH-06, SRCH-07, SRCH-08, FTEC-01, FTEC-02, FTEC-03, FTEC-05, CONF-02, CONF-03 — verify they already have [x] (they should from the audit). If any show [ ], update to [x].

    Do NOT set [x] for CI-01 through CI-08 (they were just added and need UAT verification via Phase 6).

    Note: FTEC-04 checkbox should be handled per Task 1 (moved to Out of Scope). If FTEC-04 still has a checkbox in the v1 requirements section after Task 1's edit, set it to [x] (it was SATISFIED before removal).
  </action>
  <verify>
    <automated>grep -c '\[ \]' .planning/REQUIREMENTS.md | grep -q 0 || echo "WARN: Some [ ] remain — check CI requirements (new, not yet SATISFIED)"</automated>
    <automated>grep "Status" .planning/REQUIREMENTS.md | grep -v "Complete\|In progress\|Not started" && echo "FAIL: Non-standard status found" || echo "PASS: All statuses standardized"</automated>
    <automated>grep -c '\bDone\b' .planning/REQUIREMENTS.md | grep -q 0 && echo "PASS: 'Done' removed" || echo "FAIL: 'Done' still present"</automated>
  </verify>
  <acceptance_criteria>
    - All 10 SATISFIED requirements have [x] checkboxes
    - Traceability table statuses are only: "Complete", "In progress", "Not started"
    - No "Pending" or "Done" status values remain
    - CI-01 through CI-08 retain [ ] (new, not yet UAT-verified)
  </acceptance_criteria>
  <done>All checkboxes synced, traceability statuses standardized to three-value convention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced in this phase. Work is limited to documentation and tracking artifacts. The REQUIREMENTS.md file is a planning artifact, not executable code.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-SC | Tampering | .planning/REQUIREMENTS.md | mitigate | Verify changes against CONTEXT.md decisions D-01 through D-09. All edits are tracked via git diff. |
</threat_model>

<verification>
1. Run `grep -c 'Perplexity Chat Completions' .planning/REQUIREMENTS.md` — expect 0 hits (SRCH-04 rewritten)
2. Run `grep -c 'FTEC-04' .planning/REQUIREMENTS.md` — FTEC-04 should appear only in Out of Scope table
3. Run `grep -c 'CI-0[1-8]' .planning/REQUIREMENTS.md` — expect 8 hits
4. Run `grep "Status" .planning/REQUIREMENTS.md | grep -v "Complete\|In progress\|Not started" | wc -l` — expect 0
5. All automated verify commands in tasks pass
</verification>

<success_criteria>
- [ ] SRCH-04, FTEC-04, CONF-01, CONF-03 all updated to reflect DDG-only architecture
- [ ] CI-01 through CI-08 defined in REQUIREMENTS.md and in traceability table
- [ ] PLUG-02/03 reference `.cjs` paths, SRCH-02 documents `<snippet>` tag
- [ ] Traceability statuses are: Complete / In progress / Not started only
- [ ] All 10 SATISFIED requirements have [x] checkboxes
- [ ] Whole-phase `npm test` still passes (no source code was touched)
</success_criteria>

<output>
Create `.planning/phases/08-close-tech-debt-update-requirements-md-fix-summary-gaps-fina/08-01-SUMMARY.md` when done
</output>
