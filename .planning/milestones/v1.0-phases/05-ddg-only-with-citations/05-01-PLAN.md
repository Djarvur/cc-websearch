---
phase: 05-ddg-only-with-citations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/lib/duckduckgo.ts
  - src/lib/output.ts
  - src/lib/config.ts
  - src/lib/retry.ts
  - src/lib/perplexity.ts
  - src/lib/filter.ts
  - src/websearch.ts
  - src/webfetch.ts
  - package.json
autonomous: true
requirements: [SRCH-01, SRCH-04]

must_haves:
  truths:
    - 'WebSearch returns DDG results with title, url, and snippet fields'
    - 'WebFetch returns raw markdown with no Perplexity summarization'
    - 'No Perplexity code, imports, or dependencies remain in source files'
    - 'Config contains only retry and logging sections'
    - 'Output XML always includes <snippet> tag even when empty'
  artifacts:
    - path: 'src/types.ts'
      provides: 'SearchResult with snippet field'
      contains: 'snippet'
    - path: 'src/lib/duckduckgo.ts'
      provides: 'DDG search with description extraction'
      contains: 'r.description'
    - path: 'src/lib/output.ts'
      provides: 'XML output with <snippet> tags, no provider comment'
      contains: '<snippet>'
    - path: 'src/lib/config.ts'
      provides: 'Simplified config without perplexity section'
      contains: 'retry'
    - path: 'src/websearch.ts'
      provides: 'Single-provider DDG search entry point'
      contains: 'searchDDG'
    - path: 'src/webfetch.ts'
      provides: 'Simplified fetch-to-markdown entry point'
    - path: 'src/lib/perplexity.ts'
      provides: 'DELETED - no file'
      contains: 'FILE_DELETED'
  key_links:
    - from: 'src/lib/duckduckgo.ts'
      to: 'src/types.ts'
      via: 'SearchResult.snippet field'
      pattern: 'snippet.*description'
    - from: 'src/websearch.ts'
      to: 'src/lib/duckduckgo.js'
      via: 'searchDDG import'
      pattern: 'import.*searchDDG'
    - from: 'src/webfetch.ts'
      to: 'src/lib/content.js'
      via: 'extractMarkdown import'
      pattern: 'extractMarkdown'
---

## Phase Goal

**As a** Claude Code user, **I want to** search the web and fetch pages without any API key, **so that** the plugin works out of the box with DuckDuckGo search results that include citation snippets.

<objective>
Add citation snippets (DDG descriptions) to search results and remove all Perplexity code, config, and dependencies. After this plan, the plugin is a DDG-only tool requiring zero API keys.

Purpose: Perplexity pricing is no longer viable. DDG-only with snippets delivers the same user value at zero cost.
Output: Modified source files with snippets and without Perplexity. Deleted perplexity.ts. Updated package.json.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ddg-only-with-citations/05-CONTEXT.md
@.planning/phases/05-ddg-only-with-citations/05-RESEARCH.md
@.planning/phases/05-ddg-only-with-citations/05-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types.ts (current, will be modified):

```typescript
export interface SearchResult {
  title: string;
  url: string;
}
```

From src/lib/duckduckgo.ts (current, will be modified):

```typescript
export function configureLogger(level: LogLevel): void;
export async function searchDDG(query: string): Promise<SearchResult[]>;
```

From src/lib/output.ts (current, will be modified):

```typescript
export function formatSearchResults(results: SearchResult[], provider?: string): string;
```

From src/lib/config.ts (current, will be modified):

```typescript
export interface ResolvedConfig {
  perplexity: { apiKey: string | undefined; model: string };
  retry: { maxRetries: number; baseDelay: number; maxDelay: number; timeout: number };
  logging: { level: 'debug' | 'info' | 'warn' | 'error' };
}
export function loadConfig(): ResolvedConfig;
```

From src/lib/retry.ts (current, will be modified):

```typescript
export function isTransientError(err: unknown): boolean;
export function isDDGTransientError(err: unknown): boolean;
export function getRetryConfig(config: ResolvedConfig): RetryConfig;
export async function retryWithBackoff<T>(
  fn: () => Promise<T>,
  isTransient: (err: unknown) => boolean,
  options?: Partial<RetryConfig>,
): Promise<T>;
```

From src/lib/filter.ts (buildPerplexityDomainFilter to be removed):

```typescript
export function filterByDomains(
  results: SearchResult[],
  allowedDomains?: string[],
  blockedDomains?: string[],
): SearchResult[];
export function buildPerplexityDomainFilter(
  allowedDomains?: string[],
  blockedDomains?: string[],
): string[] | undefined;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add snippet support to types, DDG provider, and output formatter</name>
  <files>src/types.ts, src/lib/duckduckgo.ts, src/lib/output.ts</files>
  <read_first>
    src/types.ts
    src/lib/duckduckgo.ts
    src/lib/output.ts
    .planning/phases/05-ddg-only-with-citations/05-CONTEXT.md (D-01 through D-05, D-13)
  </read_first>
  <behavior>
    - SearchResult type has optional `snippet?: string` field
    - searchDDG maps `r.description` from duck-duck-scrape to `snippet` field, stripping HTML tags via `replace(/<[^>]*>/g, '')`
    - searchDDG returns empty string for snippet when description is undefined/null/empty
    - formatSearchResults always includes `<snippet>` element in output, even when empty string (per D-03, D-05)
    - formatSearchResults no longer accepts `provider` parameter (per D-13)
    - formatSearchResults no longer emits `<!-- provider: X -->` comment (per D-13)
  </behavior>
  <action>
    **Per D-04:** Add `snippet?: string` field to the `SearchResult` interface in `src/types.ts`.

    **Per D-01:** In `src/lib/duckduckgo.ts`, update the `.map()` call inside `searchDDG()` to add `snippet: r.description?.replace(/<[^>]*>/g, '') || ''` to each mapped result object. The `r.description` field from duck-duck-scrape contains HTML bold tags (`<b>`) from DDG highlighting that must be stripped. Keep the existing title/url mapping unchanged. Keep the noResults early return unchanged.

    **Per D-05, D-13:** In `src/lib/output.ts`, remove the `provider?: string` parameter from `formatSearchResults`. Remove the provider comment block (`if (provider) { lines.push(...) }`). Add `<snippet>${escapeXml(result.snippet ?? '')}</snippet>` line after the `<url>` line and before `</result>`. The function signature becomes `formatSearchResults(results: SearchResult[]): string`. Keep `escapeXml` helper unchanged.

  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/cc-websearch && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - SearchResult has `snippet?: string` field
    - searchDDG extracts r.description with HTML tag stripping into snippet field
    - formatSearchResults always emits `<snippet>` element, never emits provider comment
    - formatSearchResults signature has no provider parameter
    - tsc --noEmit passes (may show errors in consumers of old signatures -- expected, fixed in Task 2)
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove Perplexity from config, retry, entry points, and dependencies</name>
  <files>src/lib/config.ts, src/lib/retry.ts, src/lib/filter.ts, src/websearch.ts, src/webfetch.ts, src/lib/perplexity.ts, package.json</files>
  <read_first>
    src/lib/config.ts
    src/lib/retry.ts
    src/lib/filter.ts
    src/websearch.ts
    src/webfetch.ts
    src/lib/perplexity.ts
    package.json
    .planning/phases/05-ddg-only-with-citations/05-CONTEXT.md (D-06 through D-16)
  </read_first>
  <action>
    **Per D-11, D-12:** In `src/lib/config.ts`:
    - Remove `perplexity` section from `ConfigSchema` (the `z.strictObject({ perplexity: ... })` block)
    - Remove `perplexity: { model: 'sonar' }` from `DEFAULTS`
    - Remove `'perplexity.apiKey': 'WEBSEARCH_PERPLEXITY_API_KEY'` and `'perplexity.model': 'WEBSEARCH_PERPLEXITY_MODEL'` from `ENV_MAP`
    - Remove `perplexity` section from `ResolvedConfig` interface
    - Remove `perplexity:` block from `loadConfig()` return object (the `apiKey:` and `model:` resolve lines)
    - Keep all retry and logging sections unchanged

    **Per D-14 removals in retry.ts:** In `src/lib/retry.ts`:
    - Remove the import block from `@perplexity-ai/perplexity_ai/error.js` (RateLimitError, InternalServerError, APIConnectionError, APIConnectionTimeoutError)
    - Remove the `isTransientError` function entirely (lines 51-57)
    - Keep `isDDGTransientError`, `retryWithBackoff`, `getRetryConfig`, `RetryConfig`, `sleep`, `withTimeout`, `configureLogger` all unchanged

    **Per research recommendation (dead code cleanup):** In `src/lib/filter.ts`:
    - Remove the `buildPerplexityDomainFilter` function entirely (lines 56-75)
    - Keep `normalizeDomain`, `matchesDomain`, `filterByDomains` unchanged

    **Per D-09, D-10:** Rewrite `src/websearch.ts` as the simplified single-provider DDG flow:
    - Remove imports: `hasApiKey, search` from `perplexity.js`, `isTransientError` from `retry.js`, `buildPerplexityDomainFilter` from `filter.js`, `SearchResult` from `types.js`, `perplexityModule` namespace import
    - Remove `dedupeAndMerge` function entirely
    - Remove entire `main()` body and replace with simplified flow: `loadConfig() -> createLogger() -> configureModuleLoggers() -> readStdin(WebSearchInputSchema) -> validateDomainExclusivity(parsed) -> retryWithBackoff(() => searchDDG(parsed.query), isDDGTransientError, getRetryConfig(config)) -> filterByDomains(results, parsed.allowed_domains, parsed.blocked_domains) -> formatSearchResults(filtered) -> stdout`
    - Remove `perplexityModule.configureLogger(level)` from `configureModuleLoggers`
    - Keep ddgModule, retryModule, fetchModule namespace imports and configureModuleLoggers
    - `formatSearchResults` call has NO provider argument (single arg)

    **Per D-06, D-07:** Simplify `src/webfetch.ts`:
    - Remove imports: `hasApiKey, summarize` from `perplexity.js`, `retryWithBackoff, getRetryConfig, isTransientError` from `retry.js`, `perplexityModule` namespace import, `loadConfig` is kept
    - Wait -- D-07 says "keep retryWithBackoff for HTTP fetch failures". But the current webfetch.ts does NOT wrap `fetchWithRedirects` in retryWithBackoff. The retry was only for Perplexity summarization. So remove retry imports entirely from webfetch.ts since there is no retry-worthy call. The fetch pipeline is: `normalizeUrl -> fetchWithRedirects -> response.text() -> extractMarkdown -> stdout`. No retry wrapping needed.
    - Remove `perplexityModule.configureLogger(level)` from `configureModuleLoggers`
    - Remove `retryModule` namespace import and `retryModule.configureLogger(level)` from configureModuleLoggers
    - In `main()`: remove the `hasApiKey(config)` check, remove the `summarize` path. The only path is: `readStdin -> normalizeUrl -> fetchWithRedirects -> response.text() -> extractMarkdown -> stdout`. Keep CrossHostRedirectError handling unchanged.

    **Per D-14:** Delete `src/lib/perplexity.ts` entirely.

    **Per D-15:** In `package.json`, remove the line `"@perplexity-ai/perplexity_ai": "0.29.0",` from dependencies.

    After all file edits, run `npm install` from the project root to update the lockfile.

    Then run `npx tsc --noEmit` to verify no type errors remain.

  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/cc-websearch && npx tsc --noEmit 2>&1 && echo "TYPE CHECK PASSED" || echo "TYPE CHECK FAILED"</automated>
  </verify>
  <done>
    - ConfigSchema has no perplexity section
    - ResolvedConfig has no perplexity section
    - loadConfig() returns only {retry, logging}
    - isTransientError function removed from retry.ts
    - buildPerplexityDomainFilter function removed from filter.ts
    - websearch.ts has single DDG path with no fallback/merge/dedupe
    - webfetch.ts has no hasApiKey/summarize/retry imports
    - src/lib/perplexity.ts deleted
    - @perplexity-ai/perplexity_ai removed from package.json
    - npm install succeeds (lockfile updated)
    - tsc --noEmit passes with zero errors
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                   |
| --------------------------------------- | --------------------------------------------- |
| stdin -> websearch/webfetch             | JSON input from Claude Code (trusted process) |
| DDG HTML response -> snippet extraction | Untrusted HTML content from DuckDuckGo        |
| DDG result descriptions -> XML output   | Untrusted text embedded in XML output         |

## STRIDE Threat Register

| Threat ID | Category               | Component                           | Disposition | Mitigation Plan                                                                                        |
| --------- | ---------------------- | ----------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------ |
| T-05-01   | Tampering              | DDG description in `<snippet>`      | mitigate    | `escapeXml()` in output.ts already escapes `& < > "`. HTML tag stripping via regex before escape.      |
| T-05-02   | Tampering              | DDG title/URL in XML output         | mitigate    | `escapeXml()` already applied to all fields including title and URL.                                   |
| T-05-03   | Information Disclosure | Removed Perplexity API key handling | accept      | Removing API key requirement improves security posture -- no secrets to leak.                          |
| T-05-SC   | Tampering              | npm install (lockfile update)       | accept      | Removing a dependency. No new packages installed. Lockfile update only removes the Perplexity package. |

</threat_model>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `grep -r "perplexity" src/ --include="*.ts"` returns zero matches (complete purge)
3. `grep -r "@perplexity-ai" src/ --include="*.ts"` returns zero matches
4. `grep -r "isTransientError" src/ --include="*.ts"` returns zero matches
5. `grep -r "buildPerplexityDomainFilter" src/ --include="*.ts"` returns zero matches
6. `test ! -f src/lib/perplexity.ts` succeeds (file deleted)
7. `npm ls @perplexity-ai/perplexity_ai 2>&1` shows "extraneous" or not found
</verification>

<success_criteria>

- All 7 verification checks pass
- tsc --noEmit clean
- Zero Perplexity references in source files
- DDG snippet extraction wired from duck-duck-scrape description through SearchResult.snippet to `<snippet>` XML tag
- websearch.ts is a clean single-provider DDG flow
- webfetch.ts is a clean fetch-extract-markdown pipeline with no LLM layer
  </success_criteria>

<output>
Create `.planning/phases/05-ddg-only-with-citations/05-01-SUMMARY.md` when done
</output>
