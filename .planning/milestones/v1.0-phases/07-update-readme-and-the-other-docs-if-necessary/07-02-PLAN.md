---
phase: 07-update-readme-and-the-other-docs-if-necessary
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - test/skills.test.ts
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - 'Full validation gate (lint + typecheck + test + build) exits with code 0'
    - 'Plugin structure validation confirms correct script paths in SKILL.md files'
    - 'Plugin structure validation confirms hooks/ directory absence is intentional'
    - 'plugin.json schema is valid with name, version, and description fields'
  artifacts:
    - path: test/skills.test.ts
      provides: 'Extended structure validation with path matching, hook absence, and description checks'
      min_lines_after: 80
  key_links:
    - from: test/skills.test.ts (script path assertions)
      to: scripts/websearch.cjs and scripts/webfetch.cjs
      via: 'extracted SKILL.md path matched against filesystem'
    - from: test/skills.test.ts (hook absence assertion)
      to: hooks/ (expected absent directory)
      via: 'existsSync check reports false'
    - from: npm run check
      to: lint, typecheck, test --coverage, and build
      via: 'package.json check script definition'
---

<objective>
Run the full verification gate on the completed project. Extend existing structure validation tests to cover script path correctness, hooks directory absence, and plugin manifest completeness. Then execute the combined validation command and fix any discovered issues.

Purpose: Confirm the plugin is production-ready for `claude plugin install` per D-13 (full validation gate) and D-14 (dry-run structure check). The automated structure tests make the verification repeatable across CI and local development.

Output:

- test/skills.test.ts (extended with path, hook, and description assertions)
- Verification gate green (npm run check exits 0)
  </objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-update-readme-and-the-other-docs-if-necessary/07-CONTEXT.md
@.planning/phases/07-update-readme-and-the-other-docs-if-necessary/07-RESEARCH.md
@.planning/phases/07-update-readme-and-the-other-docs-if-necessary/07-VALIDATION.md

<interfaces>
<!-- Existing test patterns and structure the executor must follow -->

From test/skills.test.ts (current structure):

```typescript
// Existing describe blocks: 'WebSearch SKILL.md', 'WebFetch SKILL.md', 'Compiled bundle existence'
// Helpers: parseYamlFrontmatter(content) returns Record<string, string>
// Constants: ROOT = resolve(import.meta.dirname, '..')
```

From test/manifest.test.ts (current structure):

```typescript
// 'Plugin Manifest' describe block
// Validates: valid JSON with name "cc-websearch", version matches semver
```

Expected directories to assert:

- hooks/ directory: should NOT exist (D-10: no SessionStart hook needed — intentional absence)
- scripts/ directory: should contain websearch.cjs and webfetch.cjs
- skills/websearch/ and skills/webfetch/: should each contain SKILL.md

Package.json check script:

- "check": "npm run lint && npm run typecheck && npm test -- --coverage && npm run build"
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Extend structure validation tests for path correctness, hook absence, and manifest completeness</name>
  <files>test/skills.test.ts</files>
  <read_first>
    - test/skills.test.ts (current test structure, existing assertions, parseYamlFrontmatter helper)
    - test/manifest.test.ts (existing plugin.json validation for reference)
    - skills/websearch/SKILL.md (to extract path reference)
    - skills/webfetch/SKILL.md (to extract path reference — after D-08 fix, should reference .cjs)
    - .claude-plugin/plugin.json (manifest contents to validate)
  </read_first>
  <action>
    Read current test/skills.test.ts and add three new describe blocks:

    **Block 1: "SKILL.md script path references"**
    - For each SKILL.md file (websearch and webfetch):
      - Read the file content
      - Extract the script path from the bash command line using pattern `"${CLAUDE_PLUGIN_ROOT}/scripts/([^"]+)"`
      - Assert a match was found (path reference exists)
      - Assert the extracted filename matches an existing file in `resolve(ROOT, 'scripts', extractedFilename)`
    - This validates D-09 (both skills use ${CLAUDE_PLUGIN_ROOT}) and confirms post-D-08 fix

    **Block 2: "Hooks directory"**
    - Assert that `resolve(ROOT, 'hooks')` does NOT exist on the filesystem (use existsSync)
    - If a `hooks` directory or `hooks/hooks.json` exists, the test fails
    - This validates D-10 (no SessionStart hook needed — intentional absence)

    **Block 3: "Plugin manifest description"** (extend existing manifest.test.ts or add to skills.test.ts)
    - If extending manifest.test.ts: add assertion that JSON parses and has a `description` field of type string with length > 0
    - If adding to skills.test.ts: add describe block "Plugin manifest description" with same assertion
    - Use same readFileSync + JSON.parse pattern as existing manifest tests

    Follow existing test patterns:
    - Use `import { readFileSync, existsSync } from 'node:fs'` and `import { resolve } from 'node:path'`
    - Use `ROOT = resolve(import.meta.dirname, '..')`
    - Use `describe/it/expect` from vitest
    - Match existing code style (double quotes, semicolons)

    Do NOT modify test/manifest.test.ts if adding manifest assertions to skills.test.ts — keep plugin description assertion in skills.test.ts to avoid file overlap with manifest.test.ts.

  </action>
  <verify>
    <automated>npm test -- test/skills.test.ts 2>&1 | tail -15</automated>
    <automated>npm test -- test/manifest.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All new test assertions pass: SKILL.md path extraction finds valid matches to actual compiled files in scripts/
    - Hooks directory absence assertion passes (hooks/ does not exist)
    - Plugin manifest description assertion passes (description field exists and is non-empty)
    - Existing tests still pass (no regressions to WebSearch SKILL.md, WebFetch SKILL.md, or Compiled bundle existence tests)
  </acceptance_criteria>
  <done>Structure validation tests extended with path matching, hook absence, and manifest description checks. All tests pass.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Execute full verification gate and confirm all checks pass</name>
  <files></files>
  <read_first>
    - package.json scripts section ("check" definition)
  </read_first>
  <action>
    Run `npm run check` — this executes: npm run lint && npm run typecheck && npm test -- --coverage && npm run build.

    If any step fails:
    1. Read the error output carefully
    2. Diagnose the root cause
    3. Fix the issue (could be lint error, type error, test failure, or build failure)
    4. Re-run `npm run check` from the beginning (each step depends on previous state)

    Repeat until all four steps pass with exit code 0.

    Common issues to watch for:
    - README.md might trigger Prettier/ESLint if markdown formatting is off — fix formatting, not content
    - Type errors in any test files if tsconfig.json excludes test directory incorrectly
    - Coverage thresholds if tests don't hit 80% line/branch/function/statement thresholds
    - Build failures if scripts/ output directory is missing or build.ts has an error

    Per D-13: Full validation gate — lint passes, typecheck passes, tests pass, build succeeds.
    Per D-14: Structure validation is already automated in Task 1 — dry-run structure check is satisfied by the test suite.

    Do NOT modify README.md or .env.example content to fix verification issues — only fix formatting or structural problems that prevent the gate from passing.

  </action>
  <verify>
    <automated>npm run check 2>&1</automated>
  </verify>
  <what-built>Full verification gate execution — lint, typecheck, test (with coverage), and build all passing green.</what-built>
  <how-to-verify>
    1. Run `npm run check` from the project root
    2. Confirm output shows all four steps passing:
       - ESLint + Prettier check: no errors
       - tsc --noEmit: no errors
       - vitest with coverage: all tests passing, coverage thresholds met (lines 80%, branches 70%, functions 80%, statements 80%)
       - esbuild: scripts/websearch.cjs and scripts/webfetch.cjs built successfully
    3. Confirm exit code is 0 (`echo $?`)
  </how-to-verify>
  <resume-signal>Type "approved" if verification gate is green, or describe what needs fixing.</resume-signal>
  <acceptance_criteria>
    - `npm run check` exits with code 0
    - Lint step passes: no ESLint errors, no Prettier formatting issues
    - Typecheck step passes: no tsc type errors
    - Test step passes: all vitest tests pass, coverage thresholds met
    - Build step passes: both scripts/websearch.cjs and scripts/webfetch.cjs rebuild successfully
    - Plugin is verified production-ready for `claude plugin install`
  </acceptance_criteria>
  <done>Full verification gate passes with exit code 0. Plugin distribution structure, lint, types, tests, and build are all confirmed correct.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary          | Description                                                    |
| ----------------- | -------------------------------------------------------------- |
| Test assertions   | Validation code; no user-facing impact                         |
| Verification gate | Build/test execution; modifies compiled scripts via build step |

## Threat Register

| Threat ID | Category  | Component           | Disposition | Mitigation Plan                                                          |
| --------- | --------- | ------------------- | ----------- | ------------------------------------------------------------------------ |
| T-07-04   | Tampering | test/skills.test.ts | mitigate    | All assertions are read-only (existsSync, readFileSync, string matching) |
| T-07-05   | Tampering | npm run check       | mitigate    | Standard toolchain — ESLint, tsc, vitest, esbuild all from package.json  |

</threat_model>

<verification>
- After Task 1: `npm test -- test/skills.test.ts` passes with new assertions
- After Task 2: `npm run check` exits 0
- Final: All 6 phases' worth of existing tests still pass with no regressions
</verification>

<success_criteria>

1. Structure validation tests extended and passing — path matching, hook absence, manifest description
2. Full verification gate green — lint + typecheck + test (with coverage) + build all pass
3. Plugin is verified production-ready for `claude plugin install`
4. No regression in existing tests from any prior phase
   </success_criteria>

<output>
Create `.planning/phases/07-update-readme-and-the-other-docs-if-necessary/07-02-SUMMARY.md` when done
</output>
