---
phase: 05-ddg-only-with-citations
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - test/perplexity.test.ts
  - test/duckduckgo.test.ts
  - test/output.test.ts
  - test/websearch.test.ts
  - test/config.test.ts
  - test/webfetch.test.ts
  - test/io-separation.test.ts
  - test/retry.test.ts
  - scripts/websearch.js
  - scripts/webfetch.js
autonomous: true
requirements: [SRCH-04]

must_haves:
  truths:
    - "All tests pass with zero failures"
    - "No test references perplexity mocks or imports"
    - "DDG test assertions include snippet field"
    - "Output test assertions include <snippet> tags"
    - "Websearch test covers single DDG provider flow"
  artifacts:
    - path: "test/perplexity.test.ts"
      provides: "DELETED"
      contains: "FILE_DELETED"
    - path: "test/duckduckgo.test.ts"
      provides: "Tests for snippet extraction from DDG descriptions"
      contains: "snippet"
    - path: "test/output.test.ts"
      provides: "Tests for <snippet> XML tags and no provider comment"
      contains: "snippet"
    - path: "test/websearch.test.ts"
      provides: "Tests for simplified single-provider DDG flow"
      contains: "searchDDG"
    - path: "test/config.test.ts"
      provides: "Tests for config without perplexity section"
    - path: "scripts/websearch.js"
      provides: "Rebuilt esbuild bundle"
    - path: "scripts/webfetch.js"
      provides: "Rebuilt esbuild bundle"
  key_links:
    - from: "test/duckduckgo.test.ts"
      to: "src/lib/duckduckgo.ts"
      via: "snippet field assertions"
      pattern: "snippet.*description"
    - from: "test/websearch.test.ts"
      to: "src/websearch.ts"
      via: "single-provider mock setup"
      pattern: "searchDDG.*mockResolvedValue"
---

## Phase Goal

**As a** Claude Code user, **I want to** search the web and fetch pages without any API key, **so that** the plugin works out of the box with DuckDuckGo search results that include citation snippets.

<objective>
Update all test files to match the new DDG-only source code from Plan 01. Delete perplexity tests. Update assertions for snippets. Rebuild esbuild bundles. Full test suite green.

Purpose: Tests must verify the new simplified architecture -- no Perplexity references, snippet extraction, single-provider flow.
Output: Updated test files, rebuilt bundles, zero test failures.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ddg-only-with-citations/05-CONTEXT.md
@.planning/phases/05-ddg-only-with-citations/05-RESEARCH.md
@.planning/phases/05-ddg-only-with-citations/05-PATTERNS.md
@.planning/phases/05-ddg-only-with-citations/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete perplexity tests, update DDG/output/config/retry tests</name>
  <files>test/perplexity.test.ts, test/duckduckgo.test.ts, test/output.test.ts, test/config.test.ts, test/retry.test.ts</files>
  <read_first>
    test/perplexity.test.ts
    test/duckduckgo.test.ts
    test/output.test.ts
    test/config.test.ts
    test/retry.test.ts
    src/types.ts
    src/lib/duckduckgo.ts
    src/lib/output.ts
    src/lib/config.ts
    src/lib/retry.ts
    .planning/phases/05-ddg-only-with-citations/05-CONTEXT.md (D-17, D-18, D-20, D-21)
  </read_first>
  <action>
    **Per D-17:** Delete `test/perplexity.test.ts` entirely.

    **Per D-18:** In `test/duckduckgo.test.ts`:
    - Update the existing "should return mapped title+URL results" test: change the `expect(results).toEqual([...])` assertion to include `snippet` field in each expected object. The mock results already have `description: 'desc 1'` and `description: 'desc 2'` -- assert that `snippet` matches those values (`'desc 1'`, `'desc 2'`).
    - Add new test: "should strip HTML tags from description" -- mock a result with `description: '<b>highlighted</b> text'`, assert `snippet` is `'highlighted text'`.
    - Add new test: "should return empty snippet when description is undefined" -- mock a result with no `description` field, assert `snippet` is `''`.
    - Add new test: "should return empty snippet when description is empty string" -- mock a result with `description: ''`, assert `snippet` is `''`.

    **Per D-21:** In `test/output.test.ts`:
    - Remove ALL provider comment tests (5 tests): "should include provider comment when provider argument is given", "should include duckduckgo provider comment", "should NOT include provider comment when provider is undefined", "should NOT include provider comment when provider is empty string", "should place provider comment before search_results tag".
    - Update existing tests that call `formatSearchResults` with two args to remove the second `provider` argument.
    - Add test: "should include <snippet> element with content when snippet field is provided" -- pass `{ title: 'Test', url: 'https://example.com', snippet: 'A description' }`, assert output contains `<snippet>A description</snippet>`.
    - Add test: "should include <snippet> element when snippet is undefined" -- pass `{ title: 'Test', url: 'https://example.com' }`, assert output contains `<snippet></snippet>`.
    - Add test: "should include <snippet> element when snippet is empty string" -- pass `{ title: 'Test', url: 'https://example.com', snippet: '' }`, assert output contains `<snippet></snippet>`.
    - Add test: "should escape XML entities in snippet content" -- pass `{ title: 'T', url: 'https://x.com', snippet: 'A & B < C' }`, assert output contains `<snippet>A &amp; B &lt; C</snippet>`.
    - Add test: "should place snippet tag between url and closing result tag" -- verify ordering: `<url>...</url>` then `<snippet>...</snippet>` then `</result>`.

    **Per D-20:** In `test/config.test.ts`:
    - Update "should return all default values" test: remove `config.perplexity.*` assertions. Assert only `config.retry.*` and `config.logging.level` defaults.
    - Update "should return file values when config file exists" test: remove `perplexity` from mock JSON, remove `config.perplexity.*` assertions.
    - Update "should use defaults for keys not specified in file" test: remove `config.perplexity.*` assertions.
    - Update "should resolve each key independently" test: remove `WEBSEARCH_PERPLEXITY_API_KEY` env stub, remove `config.perplexity.*` assertions.
    - Update "should use defaults for keys with neither env nor file" test: remove `WEBSEARCH_PERPLEXITY_API_KEY` env stub, remove `config.perplexity.*` assertions.
    - Delete tests: "should accept valid api key from env" and "should accept valid model from env".
    - Update "should accept a fully populated config" test: remove `perplexity` from mock object.

    In `test/retry.test.ts`:
    - Delete the entire `describe('isTransientError', ...)` block (lines 117-156) that imports from `@perplexity-ai/perplexity_ai/error.js`.
    - Update `getRetryConfig` tests: remove `perplexity` from `ResolvedConfig` mock objects -- replace `perplexity: { apiKey: undefined, model: 'sonar' },` with nothing. The config mock becomes just `{ retry: { ... }, logging: { ... } }`.
    - Keep `retryWithBackoff` and `isDDGTransientError` tests unchanged.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/cc-websearch && npx vitest run test/duckduckgo.test.ts test/output.test.ts test/config.test.ts test/retry.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - test/perplexity.test.ts deleted
    - test/duckduckgo.test.ts has 5+ tests including snippet extraction assertions
    - test/output.test.ts has no provider comment tests, has 5+ snippet tag tests
    - test/config.test.ts has zero perplexity references or assertions
    - test/retry.test.ts has no isTransientError tests, no Perplexity SDK imports
    - All 4 test files pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Update websearch/webfetch/io-separation tests, rebuild bundles, run full suite</name>
  <files>test/websearch.test.ts, test/webfetch.test.ts, test/io-separation.test.ts, scripts/websearch.js, scripts/webfetch.js</files>
  <read_first>
    test/websearch.test.ts
    test/webfetch.test.ts
    test/io-separation.test.ts
    src/websearch.ts
    src/webfetch.ts
    .planning/phases/05-ddg-only-with-citations/05-CONTEXT.md (D-19, D-06)
  </read_first>
  <action>
    **Per D-19:** Rewrite `test/websearch.test.ts` for simplified single-provider flow:
    - Remove `vi.mock('../src/lib/perplexity.js', ...)` block entirely
    - Remove `mockSearch`, `mockHasApiKey` declarations
    - Remove `mockBuildPerplexityDomainFilter` declaration
    - Update `vi.mock('../src/lib/config.js')` to return `{ retry: { maxRetries: 4, baseDelay: 1000, maxDelay: 16000, timeout: 30000 }, logging: { level: 'info' } }` (no `perplexity` key)
    - Remove `mockBuildPerplexityDomainFilter` from `vi.mock('../src/lib/filter.js')` -- only `filterByDomains` mock remains
    - Update `formatSearchResults` mock to include `<snippet>` element (no provider parameter)
    - Remove `capturedProvider` variable -- no provider tracking needed
    - Rewrite tests:
      - "should search via DDG and return results" -- verify searchDDG is called, results go to stdout
      - "should apply filterByDomains to DDG results" -- verify filterByDomains called with results and domain args
      - "should exit with error when DDG search fails" -- mock searchDDG to reject, verify error on stderr and exitCode 1
      - "should call validateDomainExclusivity with parsed input" -- verify the validation call
      - "should exit with error when both allowed and blocked domains provided" -- validateDomainExclusivity throws
      - "should not apply filtering when no domains specified" -- filterByDomains not called when no domains
      - "should pass retryWithBackoff with isDDGTransientError" -- verify the isTransient checker passed to retryWithBackoff
    - Remove ALL old tests: Perplexity-primary, fallback, merge, dedupe, provider tracking, buildPerplexityDomainFilter tests

    **Per D-06:** Update `test/webfetch.test.ts`:
    - Remove `vi.mock('../src/lib/perplexity.js', ...)` block entirely
    - Remove `mockHasApiKey`, `mockSummarize` declarations
    - Update `vi.mock('../src/lib/config.js')` to return `{ retry: { maxRetries: 4, baseDelay: 1000, maxDelay: 16000, timeout: 30000 }, logging: { level: 'info' } }` (no `perplexity` key)
    - Remove `mockRetryWithBackoff` declaration and `vi.mock('../src/lib/retry.js')` block (webfetch no longer imports retry)
    - Remove `mockRetryWithBackoff` from `beforeEach` reset list
    - Update tests:
      - "should write response text to stdout on successful fetch" -- remove `mockHasApiKey.mockReturnValue(false)` call, keep everything else
      - "should write redirect message to stdout for CrossHostRedirectError" -- keep unchanged
      - "should write error to stderr and set exitCode to 1 for generic errors" -- keep unchanged
      - "should produce error on stderr for invalid input" -- keep unchanged
      - "should write raw markdown to stdout" (rename from "when hasApiKey is false") -- remove `mockHasApiKey.mockReturnValue(false)` call, remove `expect(mockSummarize).not.toHaveBeenCalled()` assertion
    - Delete tests: "should write summarize result to stdout when hasApiKey is true", "should write error to stderr and set exitCode 1 when summarize fails after retries", "should call summarize with extracted markdown and user prompt"

    Update `test/io-separation.test.ts`:
    - Remove `vi.mock('../src/lib/perplexity.js', ...)` block entirely
    - Remove `mockSearch` declaration
    - Update `vi.mock('../src/lib/config.js')` to return `{ retry: { ... }, logging: { level: 'info' } }` (no perplexity)
    - Update `formatSearchResults` mock to include `<snippet>` element (no provider parameter)
    - Update all three tests to use `mockSearchDDG` instead of `mockSearch`
    - Update "should write to stderr only on error case" test: use `mockSearchDDG.mockRejectedValue(new Error('DDG failure'))` instead of two-provider failure pattern. The error message is now just "DDG failure", not a dual-provider message.

    After all test edits, rebuild bundles: `npm run build`

    Then run the full test suite: `npm test`
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/cc-websearch && npm test 2>&1 | tail -30</automated>
  </verify>
  <done>
    - test/websearch.test.ts has zero Perplexity mocks/imports, tests single DDG provider flow
    - test/webfetch.test.ts has zero Perplexity mocks/imports, tests raw markdown path only
    - test/io-separation.test.ts has zero Perplexity mocks/imports, uses mockSearchDDG
    - All tests pass (npm test green)
    - Bundles rebuilt (scripts/websearch.js and scripts/webfetch.js updated)
    - grep -r "perplexity" test/ --include="*.ts" returns zero matches
    - grep -r "@perplexity-ai" test/ --include="*.ts" returns zero matches
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> source | Test mocks replace source modules -- standard vitest pattern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | Test mocks hide real module behavior | accept | Standard vitest mocking -- tests verify wiring, not library behavior. Integration tests would catch real failures. |

</threat_model>

<verification>
1. `npm test` passes with zero failures
2. `grep -r "perplexity" src/ test/ --include="*.ts"` returns zero matches (complete purge across source AND tests)
3. `grep -r "@perplexity-ai" src/ test/ --include="*.ts"` returns zero matches
4. `test ! -f test/perplexity.test.ts` succeeds (test file deleted)
5. `test ! -f src/lib/perplexity.ts` succeeds (source file deleted)
6. `npm run build` succeeds (bundles rebuilt)
7. `npx tsc --noEmit` passes (type check clean)
</verification>

<success_criteria>
- All 7 verification checks pass
- Full test suite green
- Zero Perplexity references anywhere in source or test code
- Bundles rebuilt with simplified code
- Phase 5 complete -- DDG-only plugin with citation snippets
</success_criteria>

<output>
Create `.planning/phases/05-ddg-only-with-citations/05-02-SUMMARY.md` when done
</output>
