---
phase: 04-config-file-and-logging
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/lib/logger.ts
  - src/lib/retry.ts
  - src/lib/perplexity.ts
  - src/lib/duckduckgo.ts
  - src/lib/fetch.ts
  - src/websearch.ts
  - src/webfetch.ts
  - test/logger.test.ts
  - test/retry.test.ts
  - test/perplexity.test.ts
  - test/duckduckgo.test.ts
  - test/fetch.test.ts
  - test/websearch.test.ts
  - test/webfetch.test.ts
  - test/io-separation.test.ts
  - scripts/websearch.js
  - scripts/webfetch.js
autonomous: true
requirements: [CONF-02, CONF-03]

must_haves:
  truths:
    - "Logger outputs ISO 8601 timestamps and module prefixes: [timestamp] [level:module] message"
    - "Each module creates its own scoped logger via createLogger(moduleName, level)"
    - "Retry module reads config from ResolvedConfig object, not process.env directly"
    - "Perplexity module reads API key and model from ResolvedConfig object, not process.env directly"
    - "Entry points (websearch, webfetch) call loadConfig() at startup and pass config to modules"
    - "All env var references use WEBSEARCH_ prefix exclusively"
    - "All existing tests pass with updated mocks and env var stubs"
  artifacts:
    - path: "src/lib/logger.ts"
      provides: "createLogger factory function"
      exports: ["createLogger"]
      min_lines: 15
    - path: "src/lib/retry.ts"
      provides: "getRetryConfig accepting ResolvedConfig"
      contains: "ResolvedConfig"
    - path: "src/lib/perplexity.ts"
      provides: "getApiKey and hasApiKey accepting ResolvedConfig"
      contains: "ResolvedConfig"
    - path: "src/websearch.ts"
      provides: "Config-initialized entry point"
      contains: "loadConfig"
    - path: "src/webfetch.ts"
      provides: "Config-initialized entry point"
      contains: "loadConfig"
  key_links:
    - from: "src/websearch.ts"
      to: "src/lib/config.ts"
      via: "import loadConfig"
      pattern: "loadConfig"
    - from: "src/websearch.ts"
      to: "src/lib/logger.ts"
      via: "import createLogger"
      pattern: "createLogger"
    - from: "src/websearch.ts"
      to: "src/lib/retry.ts"
      via: "passing config to getRetryConfig"
      pattern: "getRetryConfig"
    - from: "src/lib/retry.ts"
      to: "src/lib/logger.ts"
      via: "import createLogger for scoped logging"
      pattern: "createLogger"
    - from: "src/lib/perplexity.ts"
      to: "src/lib/logger.ts"
      via: "import createLogger for scoped logging"
      pattern: "createLogger"
---

<objective>
Refactor logger to factory pattern, wire all consumer modules to accept config, update entry points to load config at startup, update all test files, and rebuild bundles.

Purpose: This completes the config system by connecting the config loader (Plan 01) to every module. Users can now configure the plugin via config file with structured logging output.
Output: Refactored logger.ts, retry.ts, perplexity.ts, duckduckgo.ts, fetch.ts, websearch.ts, webfetch.ts; updated test files; rebuilt bundles.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-config-file-and-logging/04-CONTEXT.md
@.planning/phases/04-config-file-and-logging/04-RESEARCH.md
@.planning/phases/04-config-file-and-logging/04-01-SUMMARY.md

@src/lib/config.ts
@src/lib/logger.ts
@src/lib/retry.ts
@src/lib/perplexity.ts
@src/lib/duckduckgo.ts
@src/lib/fetch.ts
@src/websearch.ts
@src/webfetch.ts
</context>

<interfaces>
<!-- From src/lib/config.ts (created in Plan 01) -->

```typescript
// src/lib/config.ts exports used by this plan's tasks
export interface ResolvedConfig {
  perplexity: {
    apiKey: string | undefined;
    model: string;
  };
  retry: {
    maxRetries: number;
    baseDelay: number;
    maxDelay: number;
    timeout: number;
  };
  logging: {
    level: 'debug' | 'info' | 'warn' | 'error';
  };
}

export function loadConfig(): ResolvedConfig;
```

<!-- Module name constants per D-09 -->
<!-- Modules: config, retry, perplexity, ddg, fetch, content, filter, input, output -->
<!-- Entry point modules: websearch (not in D-09 list but logical), webfetch -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor logger.ts to createLogger factory and update all lib module consumers</name>
  <files>src/lib/logger.ts, src/lib/retry.ts, src/lib/perplexity.ts, src/lib/duckduckgo.ts, src/lib/fetch.ts, test/logger.test.ts, test/retry.test.ts, test/perplexity.test.ts, test/duckduckgo.test.ts, test/fetch.test.ts</files>
  <read_first>
    src/lib/config.ts (ResolvedConfig type created in Plan 01)
    src/lib/logger.ts (current singleton -- refactor to factory)
    src/lib/retry.ts (current env var reading -- refactor to accept config)
    src/lib/perplexity.ts (current env var reading -- refactor to accept config)
    src/lib/duckduckgo.ts (imports logger -- update import to createLogger)
    src/lib/fetch.ts (imports logger -- update import to createLogger)
    test/logger.test.ts (current tests -- rewrite for factory pattern)
    test/retry.test.ts (current env var stubs -- update to WEBSEARCH_* and config-based)
    test/perplexity.test.ts (current env var stubs -- update to config-based)
    test/duckduckgo.test.ts (logger mock -- update to createLogger mock)
    test/fetch.test.ts (logger mock -- update to createLogger mock)
  </read_first>
  <behavior>
    - createLogger('test', 'info').info('hello') outputs `[ISO8601] [info:test] hello\n` to stderr
    - createLogger('test', 'warn').info('hello') produces no output (level filtering)
    - createLogger('test', 'info').setLevel('debug') then debug() produces output
    - getRetryConfig(resolvedConfig) returns values from config, not process.env
    - getApiKey(resolvedConfig) returns config.perplexity.apiKey
    - hasApiKey(resolvedConfig) returns true when config.perplexity.apiKey is set
    - searchDDG still works after duckduckgo.ts creates scoped logger via createLogger
  </behavior>
  <action>
**Step 1: Refactor src/lib/logger.ts (D-08, D-09, D-10)**

Change the export from a singleton `logger` object to a `createLogger` factory function. The function signature: `createLogger(module: string, level: LogLevel = 'info')`. Remove the module-level `let currentLevel` that reads `process.env.LOG_LEVEL`. Instead, the level is passed as a parameter.

The log format changes from `[level] message\n` to `[${timestamp}] [${level}:${module}] ${message}\n` where `timestamp = new Date().toISOString()`.

Return an object with methods: `debug`, `info`, `warn`, `error` (same as before), plus `setLevel(newLevel: LogLevel)` for runtime level changes.

Do NOT export the old `logger` singleton. The only export is `createLogger`. Also export the `LogLevel` type for consumers.

**Step 2: Update src/lib/duckduckgo.ts and src/lib/fetch.ts (logger import change only)**

These modules only use `logger.debug()`. Change their import from `import { logger } from './logger.js'` to `import { createLogger } from './logger.js'`. Create a module-scoped logger: `const logger = createLogger('ddg', 'info')` and `const logger = createLogger('fetch', 'info')`.

Note: These modules will get their log level from config when wired through the entry points in Task 2. For now, use the default `'info'` level -- the entry point will set it via `logger.setLevel()` after loading config. Alternatively, accept a logger instance as a parameter. The simpler approach (matching existing patterns): use default level and let the entry point call `setLevel()`. But since these modules create the logger at import time, the cleanest approach is to have the entry point create the logger and pass it, OR to have each module export a function that accepts config.

Simplest change: keep the module-level logger creation with default level. The entry point (Task 2) will update the level after loading config. This minimizes the refactor surface -- these modules don't read config directly, they just need the right log level.

Actually, the cleanest approach per RESEARCH.md Pattern 3: each module creates its own scoped logger at the top level with a default level. But since the level needs to come from config, and config is loaded at the entry point... The resolution: the entry point creates a "global" logger for the module and the module uses it. OR: accept that the logger is created once at import time with default level, and the entry point updates it.

Practical approach: Have duckduckgo.ts and fetch.ts continue to use a module-scoped logger created with default level. The entry point in Task 2 will create a properly-leveled logger and either: (a) call setLevel on it, or (b) these modules will be refactored to accept a logger parameter.

For minimum disruption: change the import, create the logger with default level. The level will be set properly when the entry point loads config and updates loggers (Task 2). This means these modules still have a module-level side effect, but it's the same pattern as before, just with `createLogger` instead of the old singleton.

**Step 3: Refactor src/lib/retry.ts**

Change `getRetryConfig()` to accept `config: ResolvedConfig` parameter and return values from `config.retry.*` instead of reading `process.env.RETRY_*`. Import `ResolvedConfig` type from `./config.js`. Change the logger import from `{ logger }` to `{ createLogger }` and create a module-scoped logger: `const logger = createLogger('retry', 'info')`.

Update `retryWithBackoff` -- it currently calls `getRetryConfig()` internally at line 60. Refactor so that `getRetryConfig` is only called if no explicit `options` are provided. Alternatively, change the approach: `retryWithBackoff` already accepts `options?: Partial<RetryConfig>`. Keep this interface but have the caller (entry points) always pass the config-derived values as options. This avoids retry.ts needing to import config.ts -- the entry point controls the config flow.

Approach: Leave `retryWithBackoff` signature unchanged. It already accepts `options`. The entry point (Task 2) will call `getRetryConfig(config)` and pass the result as options to `retryWithBackoff`. This means `getRetryConfig` becomes a pure utility: `getRetryConfig(config: ResolvedConfig): RetryConfig`. The `retryWithBackoff` function stays generic and config-agnostic.

Change the internal `getRetryConfig()` call on line 60: use the spread `{ ...getRetryConfig(), ...options }`. But now `getRetryConfig` needs config. Solution: `retryWithBackoff` uses a default config when no options provided. The simplest approach: `getRetryConfig` still reads from env vars as fallback for backward compat within the module, but the entry points will always pass config explicitly via the `options` parameter.

Cleanest approach per RESEARCH.md: `getRetryConfig` accepts `config: ResolvedConfig` and returns `RetryConfig`. `retryWithBackoff` stays unchanged -- callers pass config as options. The `getRetryConfig()` call on line 60 becomes unnecessary since callers always pass options. Remove the internal `getRetryConfig()` call; if no options are provided, use hardcoded defaults inline:

```typescript
const defaults: RetryConfig = { maxRetries: 4, baseDelay: 1000, maxDelay: 16000, timeout: 30000 };
const config = { ...defaults, ...options };
```

This removes the env var dependency from retry.ts entirely. The `getRetryConfig` function becomes an optional export that the entry point can use to convert `ResolvedConfig` to `RetryConfig`.

**Step 4: Refactor src/lib/perplexity.ts**

Change `getApiKey()` to accept `config: ResolvedConfig` parameter and return `config.perplexity.apiKey`. Change `hasApiKey()` to accept `config: ResolvedConfig`. Change model selection from `process.env.PPLX_MODEL || 'sonar'` to `config.perplexity.model` (already defaulted by config loader).

Change `search()` to accept `config: ResolvedConfig` as parameter. Change `summarize()` to accept `config: ResolvedConfig` as parameter. Both functions use `config.perplexity.apiKey` and `config.perplexity.model` instead of reading env vars.

Update error message from `'No API key found. Set PPLX_API_KEY or PERPLEXITY_API_KEY environment variable.'` to `'No API key found. Set WEBSEARCH_PERPLEXITY_API_KEY or add apiKey to config file.'`.

Change logger import from `{ logger }` to `{ createLogger }` and create module-scoped logger: `const logger = createLogger('perplexity', 'info')`.

**Step 5: Rewrite test/logger.test.ts**

Replace `vi.resetModules()` + dynamic import pattern with direct `createLogger()` calls. No module reset needed -- `createLogger` is a pure factory.

Test cases:
- `createLogger('test', 'info').info('hello')` outputs to stderr matching pattern `/\[\d{4}-\d{2}-\d{2}T.*\] \[info:test\] hello\n/`
- `createLogger('test', 'warn').info('hello')` produces no output (level filtering)
- `createLogger('test', 'info').error('fail')` always outputs regardless of level
- `createLogger('test', 'info').setLevel('debug')` then `debug('msg')` outputs

**Step 6: Update test/retry.test.ts**

- Change logger mock from `{ logger: { ... } }` to `{ createLogger: vi.fn(() => ({ debug: vi.fn(), info: vi.fn(), warn: vi.fn(), error: vi.fn(), setLevel: vi.fn() })) }`
- Remove `vi.stubEnv('RETRY_*', ...)` test (line 84-99) -- retry.ts no longer reads env vars directly. Replace with test that `getRetryConfig(resolvedConfig)` returns values from the config object.
- Add import of `ResolvedConfig` or create inline mock config object.
- Keep retry behavior tests (retry on transient, throw on non-transient, max retries) unchanged -- they don't depend on config.

**Step 7: Rewrite test/config.test.ts (perplexity tests)**

Wait -- `test/config.test.ts` was already rewritten in Plan 01 Task 2 for the config module tests. The perplexity tests need to move. Check if `test/perplexity.test.ts` already has its own tests. It does not exist as a separate file based on the `ls` output. The perplexity `getApiKey`/`hasApiKey` tests were in `test/config.test.ts` which was rewritten for config tests in Plan 01.

Create `test/perplexity.test.ts` with tests for the new config-based `getApiKey` and `hasApiKey`:
- `getApiKey(config_with_key)` returns the key
- `getApiKey(config_without_key)` throws with updated error message mentioning `WEBSEARCH_PERPLEXITY_API_KEY`
- `hasApiKey(config_with_key)` returns true
- `hasApiKey(config_without_key)` returns false

Update logger mock in this file from `{ logger }` to `{ createLogger: () => ({ ... }) }`.

**Step 8: Update test/duckduckgo.test.ts and test/fetch.test.ts**

- Change logger mock from `{ logger: { ... } }` to `{ createLogger: vi.fn(() => ({ debug: vi.fn(), info: vi.fn(), warn: vi.fn(), error: vi.fn(), setLevel: vi.fn() })) }`
  </action>
  <verify>
    <automated>npx vitest run test/logger.test.ts test/retry.test.ts test/perplexity.test.ts test/duckduckgo.test.ts test/fetch.test.ts test/config.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/logger.ts exports `createLogger` (not `logger` singleton)
    - Logger output format matches `[ISO8601] [level:module] message\n`
    - src/lib/retry.ts imports `createLogger` from logger, imports `ResolvedConfig` from config
    - src/lib/retry.ts `getRetryConfig` accepts `ResolvedConfig` parameter
    - src/lib/retry.ts has zero `process.env.RETRY_*` references
    - src/lib/perplexity.ts `getApiKey` accepts `ResolvedConfig` parameter
    - src/lib/perplexity.ts has zero `process.env.PPLX_*` or `process.env.PERPLEXITY_*` references
    - src/lib/perplexity.ts error message contains `WEBSEARCH_PERPLEXITY_API_KEY`
    - src/lib/duckduckgo.ts imports `createLogger` (not `logger`)
    - src/lib/fetch.ts imports `createLogger` (not `logger`)
    - All updated test files pass
    - No test file stubs old env var names (`PPLX_API_KEY`, `RETRY_*`, `LOG_LEVEL`)
    - All logger mocks export `createLogger` factory (not `logger` singleton)
  </acceptance_criteria>
  <done>Logger refactored to createLogger factory with timestamps and module prefixes. All lib modules accept config. All lib module tests updated and passing. Zero references to old env var names in source and test files.</done>
</task>

<task type="auto">
  <name>Task 2: Update entry points, remaining tests, and rebuild bundles</name>
  <files>src/websearch.ts, src/webfetch.ts, test/websearch.test.ts, test/webfetch.test.ts, test/io-separation.test.ts, scripts/websearch.js, scripts/webfetch.js</files>
  <read_first>
    src/lib/config.ts (loadConfig and ResolvedConfig from Plan 01)
    src/websearch.ts (current -- add config initialization)
    src/webfetch.ts (current -- add config initialization)
    test/websearch.test.ts (current -- update env var stubs and logger mock)
    test/webfetch.test.ts (current -- update logger mock)
    test/io-separation.test.ts (current -- update logger mock)
  </read_first>
  <action>
**Step 1: Update src/websearch.ts**

Add imports:
- `import { loadConfig } from './lib/config.js';`
- `import { createLogger } from './lib/logger.js';`

Remove the old `import { logger } from './lib/logger.js';`.

Wrap the `main()` function body to load config first:
```typescript
async function main(): Promise<void> {
  const config = loadConfig();
  const logger = createLogger('websearch', config.logging.level);
  try {
    // ... rest of main, but now pass config to modules that need it ...
  }
}
```

Update all module calls to pass config:
- `hasApiKey()` becomes `hasApiKey(config)`
- `search(parsed.query, domainFilter)` becomes `search(parsed.query, domainFilter, config)`
- `getRetryConfig()` becomes `getRetryConfig(config)` (if called directly, but currently `retryWithBackoff` calls it internally)

Since `retryWithBackoff` no longer calls `getRetryConfig` internally (changed in Task 1 to use inline defaults), the entry point must pass retry config. Update `retryWithBackoff` calls to include retry options:
```typescript
const retryOpts = getRetryConfig(config);
await retryWithBackoff(() => search(parsed.query, domainFilter, config), isTransientError, retryOpts);
```

Import `getRetryConfig` from retry module if not already imported.

**Step 2: Update src/webfetch.ts**

Same pattern as websearch.ts:
- Add `import { loadConfig } from './lib/config.js';`
- Change `import { logger } from './lib/logger.js'` to `import { createLogger } from './lib/logger.js'`
- Load config and create scoped logger at top of main()
- Pass config to `hasApiKey(config)`, `summarize(markdown, input.prompt, config)`, and `retryWithBackoff(... , getRetryConfig(config))`
- Import `getRetryConfig` from retry module

Module name for webfetch logger: `'webfetch'`.

**Step 3: Update test/websearch.test.ts**

Changes needed:
1. Logger mock: change from `{ logger: { ... } }` to `{ createLogger: vi.fn(() => ({ debug: vi.fn(), info: vi.fn(), warn: vi.fn(), error: vi.fn(), setLevel: vi.fn() })) }`
2. Add config mock: `vi.mock('../src/lib/config.js', () => ({ loadConfig: vi.fn(() => ({ perplexity: { apiKey: 'test-key', model: 'sonar' }, retry: { maxRetries: 4, baseDelay: 1000, maxDelay: 16000, timeout: 30000 }, logging: { level: 'info' } })) }))`
3. Remove `vi.stubEnv('PPLX_API_KEY', 'test-key')` and `vi.stubEnv('PPLX_MODEL', '')` from beforeEach (line 81-82) -- config mock provides these values now
4. Update any mock implementations that check for API key presence: `mockHasApiKey` is already a standalone mock, no env var dependency
5. If `getRetryConfig` is imported anywhere in the test, update the mock

**Step 4: Update test/webfetch.test.ts**

Same pattern as websearch test:
1. Logger mock: change to `createLogger` factory mock
2. Add config mock with default resolved config
3. Remove any old env var stubs

**Step 5: Update test/io-separation.test.ts**

1. Logger mock: change from `{ logger: { ... } }` to `{ createLogger: vi.fn(() => ({ debug: vi.fn(), info: vi.fn(), warn: vi.fn(), error: vi.fn(), setLevel: vi.fn() })) }`
2. Add config mock if the test imports websearch or webfetch entry points

**Step 6: Rebuild bundles**

Run `npx tsx build.ts` to rebuild `scripts/websearch.js` and `scripts/webfetch.js` with the updated source.

**Step 7: Run full test suite**

Run `npx vitest run --reporter=verbose` to verify all tests pass end-to-end.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/websearch.ts calls `loadConfig()` at the start of `main()` and creates logger via `createLogger('websearch', config.logging.level)`
    - src/websearch.ts passes config to `hasApiKey`, `search`, and `getRetryConfig`
    - src/websearch.ts has zero `process.env` references for config values
    - src/webfetch.ts calls `loadConfig()` at the start of `main()` and creates logger via `createLogger('webfetch', config.logging.level)`
    - src/webfetch.ts passes config to `hasApiKey`, `summarize`, and `getRetryConfig`
    - src/webfetch.ts has zero `process.env` references for config values
    - scripts/websearch.js and scripts/webfetch.js are rebuilt and current
    - Full test suite passes: `npx vitest run --reporter=verbose`
    - No test file stubs old env var names (`PPLX_*`, `RETRY_*`, `LOG_LEVEL`)
    - All logger mocks across all test files use `createLogger` factory pattern
  </acceptance_criteria>
  <done>Entry points load config at startup and pass to all modules. All tests pass. Bundles rebuilt. Zero old env var references in source code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file -> config module | User-controlled file content (validated in Plan 01) |
| config module -> lib modules | Resolved config object passed as parameter |
| env vars -> config module | User-controlled strings (coerced and validated in Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | API key in config object | accept | Config object exists in memory only during process lifetime. CLI tool, no persistence. |
| T-04-06 | Tampering | Module logger level | accept | Level comes from validated config. No user input affects level after startup. |
</threat_model>

<verification>
# Run full test suite
npx vitest run --reporter=verbose

# Verify no old env var references in source
grep -rn 'PPLX_API_KEY\|PERPLEXITY_API_KEY\|PPLX_MODEL\|RETRY_MAX_RETRIES\|RETRY_BASE_DELAY\|RETRY_MAX_DELAY\|RETRY_TIMEOUT\|LOG_LEVEL' src/ && echo "FAIL: old env vars found" || echo "OK: no old env vars"

# Verify createLogger is exported from logger
grep -c 'export function createLogger' src/lib/logger.ts

# Verify loadConfig is called in entry points
grep -c 'loadConfig' src/websearch.ts src/webfetch.ts
</verification>

<success_criteria>
- Logger outputs ISO 8601 timestamps and module prefixes
- createLogger factory used in all modules (no singleton)
- All lib modules accept config object instead of reading env vars directly
- Entry points load config once at startup and pass to modules
- All WEBSEARCH_* env vars functional (override config file values)
- All existing tests pass with updated mocks and env var stubs
- Bundles rebuilt and current
- Zero references to old env var names (PPLX_*, RETRY_*, LOG_LEVEL) in source code
</success_criteria>

<output>
Create `.planning/phases/04-config-file-and-logging/04-02-SUMMARY.md` when done
</output>
