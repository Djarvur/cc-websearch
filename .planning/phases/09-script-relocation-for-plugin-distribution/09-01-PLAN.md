---
phase: 09-script-relocation-for-plugin-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.ts
  - skills/websearch/SKILL.md
  - skills/webfetch/SKILL.md
  - test/skills.test.ts
  - test/e2e/helpers.ts
  - README.md
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03, DIST-04, DIST-05, DIST-06]

must_haves:
  truths:
    - "bundles are created at skills/websearch/scripts/websearch.cjs and skills/webfetch/scripts/webfetch.cjs"
    - "old scripts/ root directory no longer exists"
    - "SKILL.md files reference new bundle locations"
    - "no dangling references to old scripts/ paths in any file"
    - "npm run build && npm run check pass clean"
  artifacts:
    - path: "skills/websearch/scripts/websearch.cjs"
      provides: "WebSearch bundle in skill directory"
      contains: "esbuild bundle"
    - path: "skills/webfetch/scripts/webfetch.cjs"
      provides: "WebFetch bundle in skill directory"
      contains: "esbuild bundle"
  key_links:
    - from: "skills/websearch/SKILL.md"
      to: "skills/websearch/scripts/websearch.cjs"
      pattern: 'CLAUDE_PLUGIN_ROOT\}/skills/websearch/scripts/websearch\.cjs'
    - from: "skills/webfetch/SKILL.md"
      to: "skills/webfetch/scripts/webfetch.cjs"
      pattern: 'CLAUDE_PLUGIN_ROOT\}/skills/webfetch/scripts/webfetch\.cjs'
---

<objective>
Relocate compiled esbuild bundles from root `scripts/` into skill subdirectories. Each skill becomes self-contained with its own script — `claude plugin install` distributes everything automatically.

Purpose: Plugin distribution copies entire directory tree. Currently scripts live at root `scripts/` and must be referenced via static path. Moving them into `skills/<name>/scripts/` ensures they ship with their skill definition.
Output: Self-contained skill directories, removed old scripts/ root, all path references updated.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/09-script-relocation-for-plugin-distribution/09-CONTEXT.md
@.planning/phases/09-script-relocation-for-plugin-distribution/09-RESEARCH.md

**Current build.ts structure:**
- `outdir: 'scripts'` emits both bundles to scripts/
- `entryPoints: ['src/websearch.ts', 'src/webfetch.ts']`
- `format: 'cjs'`, `outExtension: { '.js': '.cjs' }`

**Files to update:**
- `build.ts` — Change output paths per D-01
- `skills/websearch/SKILL.md` — Update command path per D-02
- `skills/webfetch/SKILL.md` — Update command path per D-03
- `test/skills.test.ts` — Update bundle path assertions per D-06
- `test/e2e/helpers.ts` — Update spawn path constant per D-07
- `README.md` — Update example paths per D-03

**After relocation:** `git rm -r scripts/` per D-04
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update build.ts for per-skill output</name>
  <files>build.ts</files>
  <action>
    Change esbuild configuration to output each bundle to its skill subdirectory instead of root scripts/.

    Replace the single `outdir: 'scripts'` with per-entry-point output paths using esbuild's `outExtension` and separate `entryNames` or use separate `build` calls.

    Option A (simpler): Use two separate `esbuild.build()` calls — one per skill — with their own `outdir`:
    - websearch entry → outdir: 'skills/websearch/scripts'
    - webfetch entry → outdir: 'skills/webfetch/scripts'
    
    Keep all other config identical: `bundle: true`, `platform: 'node'`, `format: 'cjs'`, `outExtension: { '.js': '.cjs' }`, `external: ['jsdom']`.

    Option B (if preferred): Single build call with custom plugin to map entry points to output paths.

    Ensure `npm run build` produces:
    - `skills/websearch/scripts/websearch.cjs` (NOT scripts/websearch.cjs)
    - `skills/webfetch/scripts/webfetch.cjs` (NOT scripts/webfetch.cjs)
    - Old scripts/ files may linger — do not delete them here (Task 4 handles cleanup)
  </action>
  <verify>
    <automated>npm run build && test -f skills/websearch/scripts/websearch.cjs && test -f skills/webfetch/scripts/webfetch.cjs</automated>
  </verify>
  <done>Bundles exist at new paths, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md and README paths</name>
  <files>skills/websearch/SKILL.md, skills/webfetch/SKILL.md, README.md</files>
  <action>
    Update script reference paths in all three files.

    1. In `skills/websearch/SKILL.md`:
       Change `${CLAUDE_PLUGIN_ROOT}/scripts/websearch.cjs` to `${CLAUDE_PLUGIN_ROOT}/skills/websearch/scripts/websearch.cjs`

    2. In `skills/webfetch/SKILL.md`:
       Change `${CLAUDE_PLUGIN_ROOT}/scripts/webfetch.cjs` to `${CLAUDE_PLUGIN_ROOT}/skills/webfetch/scripts/webfetch.cjs`

    3. In `README.md`:
       Update both script paths in the usage examples (lines ~27 and ~54). Update installation section (line ~13) if it references scripts/ directory.

    Also search for any other `${CLAUDE_PLUGIN_ROOT}/scripts/` references in CLAUDE.md and update those too.
  </action>
  <verify>
    <automated>grep -r "CLAUDE_PLUGIN_ROOT}/scripts/" . --include="*.md" --include="*.json" | grep -v node_modules || echo "No old paths found — clean"</automated>
  </verify>
  <done>All references point to new skill subdirectory paths</done>
</task>

<task type="auto">
  <name>Task 3: Update tests with new bundle paths</name>
  <files>test/skills.test.ts, test/e2e/helpers.ts</files>
  <action>
    Update path-dependent test assertions to reference new bundle locations.

    1. In `test/skills.test.ts`:
       Find any assertions that check for `scripts/websearch.cjs` or `scripts/webfetch.cjs`.
       Update to `skills/websearch/scripts/websearch.cjs` and `skills/webfetch/scripts/webfetch.cjs`.
       There should be checks for bundle file existence — update the paths.

    2. In `test/e2e/helpers.ts`:
       Find the `runScript` or similar helper that constructs paths to bundled scripts.
       Update the base path from `../../scripts/` to `../../skills/<name>/scripts/`.
       The helper likely uses `__dirname` relative path resolution.
       Read the file first to see exact path construction.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -10</automated>
  </verify>
  <done>All tests pass with new bundle locations</done>
</task>

<task type="auto">
  <name>Task 4: Remove old scripts/ directory and verify</name>
  <files>.gitignore, scripts/websearch.cjs, scripts/webfetch.cjs</files>
  <action>
    After confirming all references are updated:

    1. Grep for any remaining references to `scripts/websearch` or `scripts/webfetch` (excluding the new skill subdirectory paths and node_modules):
       ```bash
       grep -rn "scripts/websearch\|scripts/webfetch" . --include="*" ! -path "*/skills/*" ! -path "*/node_modules/*" ! -path "*/.git/*" || true
       ```
       If any non-skill references remain, update them before proceeding.

    2. Remove the old scripts directory:
       ```bash
       git rm -r scripts/
       ```

    3. Check `.gitignore` — if it has path-specific entries for `scripts/*.cjs`, they may no longer apply. Adjust or remove them.
  </action>
  <verify>
    <automated>test ! -f scripts/websearch.cjs && test ! -f scripts/webfetch.cjs</automated>
  </verify>
  <done>Old scripts/ directory removed, no dangling references</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds and creates bundles at new paths
2. `npm test` — all 142 tests pass, 0 errors
3. `npm run lint` — ESLint + Prettier clean
4. `npm run e2e` — all 5 E2E tests pass
5. `grep -r "CLAUDE_PLUGIN_ROOT}/scripts/" . --include="*" | grep -v node_modules` — zero matches
</verification>

<success_criteria>
- DIST-01 ✓: Bundles at skills/websearch/scripts/websearch.cjs and skills/webfetch/scripts/webfetch.cjs
- DIST-02 ✓: SKILL.md references use new paths
- DIST-03 ✓: README examples updated
- DIST-04 ✓: Path-dependent tests pass
- DIST-05 ✓: Old scripts/ root removed
- DIST-06 ✓: npm run build && npm run check pass
</success_criteria>

<output>
Create `09-01-SUMMARY.md` when done
</output>
