---
phase: 06-ci-pipeline-and-e2e-tests
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - .github/workflows/cron.yml
  - .github/dependabot.yml
autonomous: true
requirements:
  - CI-03
  - CI-08

must_haves:
  truths:
    - "Periodic cron workflow runs npm audit and E2E tests on a weekly schedule"
    - "Cron workflow allows manual triggering via workflow_dispatch"
    - "Dependabot creates automated PRs for npm dependency updates weekly"
    - "Dependabot creates automated PRs for GitHub Actions version updates weekly"
  artifacts:
    - path: ".github/workflows/cron.yml"
      provides: "Periodic workflow for npm audit + E2E tests"
      contains: "npm audit"
    - path: ".github/dependabot.yml"
      provides: "Dependabot config for npm + github-actions ecosystems"
      contains: "package-ecosystem"
  key_links:
    - from: ".github/workflows/cron.yml"
      to: "package.json"
      via: "npm run e2e script"
      pattern: "npm run e2e"
    - from: ".github/dependabot.yml"
      to: "package.json"
      via: "npm ecosystem updates"
      pattern: "package-ecosystem.*npm"
---

<objective>
Create the periodic cron workflow for security auditing and E2E tests, plus Dependabot configuration for automated dependency updates.

Purpose: Keeps the project secure (npm audit) and dependencies fresh (Dependabot) without manual tracking. E2E tests in cron catch regressions against live services between releases.
Output: Cron workflow and Dependabot config committed.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ci-pipeline-and-e2e-tests/06-CONTEXT.md
@.planning/phases/06-ci-pipeline-and-e2e-tests/06-RESEARCH.md
@.planning/phases/06-ci-pipeline-and-e2e-tests/06-PATTERNS.md
@.planning/phases/06-ci-pipeline-and-e2e-tests/06-01-SUMMARY.md

<interfaces>
<!-- From package.json (scripts added by Plan 01) -->
```json
"e2e": "npm run build && vitest run --config vitest.config.e2e.ts test/e2e/"
```

<!-- From Plan 02 (ci.yml structure for consistency) -->
- Node 20, ubuntu-latest, actions/checkout@v4, actions/setup-node@v4 with npm cache
- npm ci for deterministic installs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cron workflow and Dependabot config</name>
  <files>.github/workflows/cron.yml, .github/dependabot.yml</files>
  <read_first>
    .github/workflows/ci.yml (for consistency with PR gate workflow patterns)
    .planning/phases/06-ci-pipeline-and-e2e-tests/06-RESEARCH.md (Pattern 6, cron workflow example)
    .planning/phases/06-ci-pipeline-and-e2e-tests/06-PATTERNS.md (Pattern assignments for cron.yml and dependabot.yml)
  </read_first>
  <action>
Per D-09 (two workflows), D-12 (cron: audit + E2E), D-13 (Dependabot config).

Step 1: Create .github/workflows/cron.yml per RESEARCH.md and PATTERNS.md.

Workflow structure:
- name: Periodic Checks
- on: schedule with cron '0 6 * * 1' (Monday 6 AM UTC), plus workflow_dispatch for manual triggers
- jobs.audit runs-on: ubuntu-latest
- Steps:
  1. actions/checkout@v4
  2. actions/setup-node@v4 with node-version: 20, cache: 'npm'
  3. npm ci
  4. npm audit --audit-level=high (fails on high/critical vulnerabilities per D-12)
  5. npm run build (E2E tests need bundles)
  6. npm run e2e with timeout-minutes: 10 (real network calls can be slow)
- No continue-on-error. Audit failure = job failure.

Step 2: Create .github/dependabot.yml per D-13 and RESEARCH.md Pattern 6.

Config structure:
- version: 2
- updates array with two entries:
  1. package-ecosystem: "npm", directory: "/", schedule interval: "weekly", commit-message prefix: "deps" with include: "scope", open-pull-requests-limit: 5
  2. package-ecosystem: "github-actions", directory: "/", schedule interval: "weekly"

Step 3: Validate both files are syntactically correct. Run `node -e` checks for key strings in each file.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('.github/workflows/cron.yml','utf8'); const d=require('fs').readFileSync('.github/dependabot.yml','utf8'); console.assert(c.includes('npm audit'), 'audit found'); console.assert(c.includes('npm run e2e'), 'e2e found'); console.assert(c.includes('workflow_dispatch'), 'dispatch found'); console.assert(d.includes('version: 2'), 'version found'); console.assert(d.includes('package-ecosystem'), 'ecosystem found'); console.assert(d.includes('npm'), 'npm ecosystem found'); console.assert(d.includes('github-actions'), 'actions ecosystem found'); console.log('Both files validated')"</automated>
  </verify>
  <done>
    - .github/workflows/cron.yml exists with weekly schedule + workflow_dispatch trigger
    - Cron workflow runs: checkout, setup-node (v20), npm ci, npm audit (high/critical), npm run build, npm run e2e (10min timeout)
    - .github/dependabot.yml exists with version: 2
    - Dependabot configured for npm ecosystem (weekly, deps prefix, limit 5) and github-actions ecosystem (weekly)
    - Both files are syntactically valid YAML
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dependabot -> npm registry | Automated dependency updates from external registry |
| Cron workflow -> npm registry | npm audit checks vulnerability database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | Dependabot PRs | mitigate | Dependabot creates PRs but does not auto-merge. PR gate CI validates each dependency update before merge. |
| T-06-06 | Tampering | npm audit false negatives | accept | npm audit database may not cover all vulnerabilities. Weekly cadence provides reasonable coverage. |
</threat_model>

<verification>
1. cron.yml has schedule trigger with cron syntax
2. cron.yml has workflow_dispatch trigger
3. cron.yml runs npm audit --audit-level=high
4. cron.yml runs npm run e2e with timeout-minutes: 10
5. dependabot.yml has version: 2
6. dependabot.yml has both npm and github-actions ecosystems
7. Both files parse as valid YAML
</verification>

<success_criteria>
- Cron workflow configured with weekly Monday 6AM UTC schedule + manual trigger
- Cron runs npm audit (fails on high/critical) and E2E tests (10min timeout)
- Dependabot configured for npm + github-actions weekly updates
- Dependabot PRs limited to 5 open at a time, prefixed with "deps"
</success_criteria>

<output>
Create `.planning/phases/06-ci-pipeline-and-e2e-tests/06-03-SUMMARY.md` when done
</output>
