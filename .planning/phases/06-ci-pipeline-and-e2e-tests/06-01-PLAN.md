---
phase: 06-ci-pipeline-and-e2e-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - build.ts
  - vitest.config.ts
  - eslint.config.js
  - .prettierrc
  - .prettierignore
  - .mise.toml
autonomous: true
requirements:
  - CI-04
  - CI-05
  - CI-06
  - CI-07

must_haves:
  truths:
    - 'npm run lint runs ESLint + Prettier check and exits non-zero on violations'
    - 'npm run typecheck passes with zero errors'
    - 'npm test -- --coverage runs unit tests and enforces coverage thresholds'
    - 'npm run build produces working bundles (websearch.cjs and webfetch.cjs)'
    - 'npm run check runs lint + typecheck + test + coverage + build in sequence'
    - 'mise run check-all mirrors all CI steps locally'
  artifacts:
    - path: 'eslint.config.js'
      provides: 'ESLint 9 flat config with TypeScript + Prettier'
      contains: 'typescript-eslint'
    - path: '.prettierrc'
      provides: 'Prettier formatting config'
      contains: 'singleQuote'
    - path: '.mise.toml'
      provides: 'mise task definitions mirroring CI steps'
      contains: 'check-all'
    - path: 'vitest.config.ts'
      provides: 'Coverage thresholds and E2E test exclusion'
      contains: 'thresholds'
    - path: 'build.ts'
      provides: 'jsdom external for webfetch bundle fix'
      contains: 'external'
  key_links:
    - from: 'eslint.config.js'
      to: 'package.json'
      via: 'devDependencies (eslint, typescript-eslint, prettier)'
      pattern: 'eslint.*typescript-eslint'
    - from: 'vitest.config.ts'
      to: 'package.json'
      via: 'devDependency @vitest/coverage-v8'
      pattern: 'coverage.*provider.*v8'
---

<objective>
Set up local CI toolchain: ESLint with flat config, Prettier, coverage enforcement, mise task runner, and fix the jsdom bundle issue.

Purpose: Establishes the quality gate that all subsequent work must pass. Fixes the deferred jsdom bundle bug so WebFetch E2E tests can work. After this plan, the developer can run `npm run check` locally and get the same validation CI will enforce.
Output: Working lint, format, coverage, build, and mise configurations. All existing code passes.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ci-pipeline-and-e2e-tests/06-CONTEXT.md
@.planning/phases/06-ci-pipeline-and-e2e-tests/06-RESEARCH.md
@.planning/phases/06-ci-pipeline-and-e2e-tests/06-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From package.json (current state):

```json
{
  "name": "cc-websearch",
  "type": "module",
  "scripts": {
    "build": "tsx build.ts",
    "test": "vitest run --reporter=verbose",
    "test:watch": "vitest --reporter=verbose",
    "typecheck": "tsc --noEmit"
  }
}
```

From vitest.config.ts (current state, 7 lines):

```typescript
import { defineConfig } from 'vitest/config';
export default defineConfig({
  test: { include: ['test/**/*.test.ts'] },
});
```

From build.ts (current state, 23 lines):

```typescript
import { build } from 'esbuild';
const commonOptions = {
  bundle: true,
  platform: 'node' as const,
  target: 'node20',
  format: 'cjs' as const,
  outExtension: { '.js': '.cjs' },
  banner: { js: '#!/usr/bin/env node' },
};
await Promise.all([
  build({ ...commonOptions, entryPoints: ['src/websearch.ts'], outfile: 'scripts/websearch.cjs' }),
  build({ ...commonOptions, entryPoints: ['src/webfetch.ts'], outfile: 'scripts/webfetch.cjs' }),
]);
```

From tsconfig.json:

- `"type": "module"` in package.json means eslint.config.js is ESM (no .mjs needed)
- `"include": ["src/**/*.ts", "test/**/*.ts", "build.ts]`
- `"exclude": ["node_modules", "dist", "scripts"]`
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install devDependencies, create config files, fix jsdom bundle, update vitest and scripts</name>
  <files>package.json, eslint.config.js, .prettierrc, .prettierignore, build.ts, vitest.config.ts</files>
  <read_first>
    package.json
    build.ts
    vitest.config.ts
    tsconfig.json
    .planning/phases/06-ci-pipeline-and-e2e-tests/06-RESEARCH.md (Patterns 1, 3, anti-pattern "Bundling jsdom")
    .planning/phases/06-ci-pipeline-and-e2e-tests/06-PATTERNS.md (Pattern assignments for eslint.config.js, vitest.config.ts, build.ts, .prettierrc)
  </read_first>
  <behavior>
    - `npx eslint .` processes src/ and test/ files with typescript-eslint recommended + strict rules
    - `npx prettier --check .` validates formatting without writing
    - `npm test -- --coverage` reports coverage and enforces thresholds (80% lines, 70% branches, 80% functions)
    - `npm run build` produces scripts/webfetch.cjs that does NOT crash on load (jsdom external fix)
    - E2E test directory (test/e2e/) is excluded from unit test coverage
  </behavior>
  <action>
  Per D-05, D-06, D-07, D-14, D-15, and the jsdom fix requirement.

Step 1: Install devDependencies. Run: `npm install --save-dev eslint @eslint/js typescript-eslint eslint-config-prettier prettier @vitest/coverage-v8`. All six packages are [ASSUMED] per RESEARCH.md audit table. They are well-established packages (eslint ~50M/wk, prettier ~40M/wk) with no postinstall scripts and verifiable source repos. The RESEARCH.md audit confirms no [SLOP] or [SUS] packages. Proceed with install.

Step 2: Create eslint.config.js per D-05 (ESLint 9 flat config) and D-06 (recommended + strict). Follow RESEARCH.md Pattern 1 and PATTERNS.md assignment exactly: import js from @eslint/js, defineConfig from eslint/config, tseslint from typescript-eslint, eslintConfigPrettier from eslint-config-prettier. Export defineConfig with js.configs.recommended, spread tseslint.configs.recommended, spread tseslint.configs.strict, an ignores block for scripts/**, node_modules/**, coverage/**, dist/**, and eslintConfigPrettier as the LAST entry (D-07). Project has "type": "module" in package.json so eslint.config.js is ESM -- no .mjs needed.

Step 3: Create .prettierrc with config values: semi: true, singleQuote: true, trailingComma: "all", printWidth: 100, tabWidth: 2. Create .prettierignore with entries: scripts/, coverage/, node_modules/, dist/.

Step 4: Fix build.ts per PATTERNS.md assignment. Add `external: ['jsdom']` to the webfetch build entry ONLY. This resolves the deferred jsdom bundle issue (default-stylesheet.css ENOENT). The websearch entry stays unchanged. Add the comment `// jsdom must be external -- reads CSS via fs at runtime` on the external line.

Step 5: Update vitest.config.ts per D-14, D-15 and PATTERNS.md assignment. Add `exclude: ['test/e2e/**']` to keep E2E tests out of unit coverage. Add the full coverage block: provider: 'v8', reporter: ['text', 'lcov'], include: ['src/**/*.ts'], exclude: ['src/types.ts'], thresholds: { lines: 80, branches: 70, functions: 80, statements: 80 }. Keep the existing include pattern unchanged.

Step 6: Add scripts to package.json per PATTERNS.md: "lint" = "eslint . && prettier --check .", "e2e" = "npm run build && vitest run --config vitest.config.e2e.ts test/e2e/", "check" = "npm run lint && npm run typecheck && npm test -- --coverage && npm run build". Keep all existing scripts (build, test, test:watch, typecheck) unchanged.

Step 7: Run `npm run build` to regenerate bundles with the jsdom external fix. Verify `node -e "require('./scripts/webfetch.cjs')"` does NOT crash (the jsdom import should resolve from node_modules). NOTE: The require will hang waiting for stdin -- that is expected success. A crash with ENOENT for default-stylesheet.css would be failure.
</action>
<verify>
<automated>npm run build && node -e "setTimeout(() => { console.log('webfetch.cjs loaded without ENOENT'); process.exit(0); }, 2000)" &(require_pid=$!; node -e "const c=require('./scripts/webfetch.cjs')" 2>&1 | head -5; kill $require_pid 2>/dev/null) || echo "Bundle load test: checking for ENOENT..."; test "$(node -e "try { require('./scripts/webfetch.cjs'); } catch(e) { if(e.message.includes('ENOENT')) { process.exit(1); } }" 2>&1 | grep -c ENOENT)" = "0" && echo "No ENOENT -- bundle OK"</automated>
</verify>
<done> - eslint.config.js exists with flat config, typescript-eslint recommended + strict, eslint-config-prettier last - .prettierrc exists with singleQuote, semi, trailingComma, printWidth, tabWidth - .prettierignore exists with scripts/, coverage/, node_modules/, dist/ - vitest.config.ts has coverage thresholds (80/70/80) and excludes test/e2e/\*\* - build.ts marks jsdom as external for webfetch entry - package.json has lint, e2e, check scripts - npm run build produces webfetch.cjs that does not crash on load (ENOENT resolved)
</done>
</task>

<task type="auto">
  <name>Task 2: Lint cleanup pass, Prettier formatting, and full verification suite</name>
  <files>package.json, build.ts, vitest.config.ts, eslint.config.js, .prettierrc, .prettierignore</files>
  <read_first>
    eslint.config.js (created in Task 1)
    .prettierrc (created in Task 1)
    .planning/phases/06-ci-pipeline-and-e2e-tests/06-RESEARCH.md (Pitfall 4: ESLint strict cleanup, Pitfall 3: coverage thresholds)
  </read_first>
  <action>
  Per D-06 (strict rules may require cleanup). This task takes the configs created in Task 1 and ensures all existing code passes.

Step 1: Run `npm run lint` on existing code. Expect lint errors from strict mode (D-06 acknowledged this). Fix ONLY the errors, not warnings. Common fixes: prefix unused vars with underscore, add explicit return types where missing, remove console.log statements. Do NOT disable strict rules globally. If a specific rule produces excessive noise on existing code, add an inline disable comment with an explanation.

Step 2: Run `npx prettier --write .` to format all files according to the new .prettierrc. This is a one-time formatting pass.

Step 3: Run `npm test` to verify all 127 existing tests still pass after lint fixes and formatting.

Step 4: Run `npm run typecheck` to verify no type errors were introduced.

Step 5: Run `npm test -- --coverage` to check current coverage against the new thresholds. If coverage is below thresholds, adjust thresholds DOWN to match reality (per RESEARCH.md Pitfall 3). Document the actual baseline percentages in the SUMMARY. Do NOT add new tests to meet thresholds -- that is not this plan's job.

Step 6: Run `npm run check` as the final verification -- this chains lint + typecheck + test with coverage + build. Must exit 0.
</action>
<verify>
<automated>npm run lint && npm run typecheck && npm test -- --coverage && npm run build</automated>
</verify>
<done> - npm run lint exits 0 on existing code (all strict errors fixed, warnings acceptable) - All code formatted with Prettier - npm test passes all existing tests - npm run typecheck exits 0 - Coverage thresholds set to baseline (at or below actual coverage) - npm run check passes the full chain: lint + typecheck + test with coverage + build
</done>
</task>

<task type="auto">
  <name>Task 3: Create mise task runner config and verify full local CI parity</name>
  <files>.mise.toml</files>
  <read_first>
    package.json (for current scripts section)
    .planning/phases/06-ci-pipeline-and-e2e-tests/06-RESEARCH.md (Pattern 4)
    .planning/phases/06-ci-pipeline-and-e2e-tests/06-PATTERNS.md (Pattern assignment for .mise.toml)
  </read_first>
  <action>
  Per D-08. Create .mise.toml with six task definitions that mirror CI steps and package.json scripts.

Tasks to define (per RESEARCH.md Pattern 4):

1. `lint` -- description: "Run ESLint and Prettier checks", run: "npm run lint"
2. `typecheck` -- description: "TypeScript type checking", run: "npm run typecheck"
3. `test` -- description: "Run unit tests with coverage", run: "npm test -- --coverage"
4. `build` -- description: "Build bundles", run: "npm run build"
5. `e2e` -- description: "Run end-to-end tests", run: "npm run e2e"
6. `check-all` -- description: "Run all CI checks locally", depends: ["lint", "typecheck", "test", "build"], run: "echo 'All checks passed'"

All tasks delegate to npm scripts. mise is NOT installed on the dev machine so these tasks are for users who have mise. npm scripts are the primary interface.

After creating .mise.toml, run `npm run check` to verify the full CI chain passes: lint + typecheck + test with coverage + build.
</action>
<verify>
<automated>npm run check</automated>
</verify>
<done> - .mise.toml exists with 6 tasks: lint, typecheck, test, build, e2e, check-all - check-all depends on lint, typecheck, test, build (parallel by default in mise) - npm run check passes: lint, typecheck, unit tests with coverage, build -- all green
</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                  |
| ------------------------------- | -------------------------------------------- |
| npm registry -> node_modules    | Package installation from external registry  |
| dev tool configs -> source code | Linter/formatter rules affect code execution |

## STRIDE Threat Register

| Threat ID | Category  | Component                | Disposition | Mitigation Plan                                                                                                                  |
| --------- | --------- | ------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------- |
| T-06-01   | Tampering | npm install (6 devDeps)  | mitigate    | All packages verified: no postinstall scripts, established source repos, millions of weekly downloads per RESEARCH.md audit      |
| T-06-02   | Tampering | ESLint config evaluation | accept      | eslint.config.js runs in Node.js context; standard tool config, no elevated privileges                                           |
| T-06-SC   | Tampering | npm/pip installs         | mitigate    | All 6 packages [ASSUMED] per RESEARCH.md audit -- verified via registry checks, no postinstall scripts, no [SLOP]/[SUS] verdicts |

</threat_model>

<verification>
1. `npm run lint` exits 0
2. `npm run typecheck` exits 0
3. `npm test -- --coverage` passes with coverage above thresholds
4. `npm run build` succeeds and webfetch.cjs loads without ENOENT
5. `npm run check` runs all of the above in sequence and exits 0
6. .mise.toml has all 6 task definitions
</verification>

<success_criteria>

- All existing code passes ESLint recommended + strict rules
- All code formatted with Prettier
- Coverage thresholds configured and passing (may be adjusted to match baseline)
- jsdom bundle bug fixed -- webfetch.cjs loads without ENOENT
- `npm run check` is a single command that validates everything CI will check
- .mise.toml mirrors all CI steps for local parity
  </success_criteria>

<output>
Create `.planning/phases/06-ci-pipeline-and-e2e-tests/06-01-SUMMARY.md` when done
</output>
