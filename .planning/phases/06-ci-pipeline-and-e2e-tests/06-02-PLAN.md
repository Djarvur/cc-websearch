---
phase: 06-ci-pipeline-and-e2e-tests
plan: 02
type: execute
wave: 2
depends_on: ['06-01']
files_modified:
  - test/e2e/helpers.ts
  - test/e2e/websearch.e2e.ts
  - test/e2e/webfetch.e2e.ts
  - vitest.config.e2e.ts
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - CI-01
  - CI-02
  - CI-04
  - CI-06

must_haves:
  truths:
    - 'WebSearch E2E test runs node scripts/websearch.cjs with stdin JSON and validates XML output structure'
    - 'WebFetch E2E test runs node scripts/webfetch.cjs with stdin JSON and validates markdown output'
    - 'Error handling E2E tests verify stderr output and non-zero exit codes for invalid input'
    - 'Domain filtering E2E test validates allowed_domains/blocked_domains end-to-end'
    - 'E2E tests retry up to 3 times on transient network failures'
    - 'GitHub Actions PR gate triggers on push to master and pull requests to master'
    - 'PR gate runs lint, typecheck, unit tests with coverage, and build -- all must pass'
  artifacts:
    - path: 'test/e2e/helpers.ts'
      provides: 'runScript (spawn child process) and withRetry utilities'
      exports: ['runScript', 'withRetry']
    - path: 'test/e2e/websearch.e2e.ts'
      provides: 'E2E tests for WebSearch: basic query, domain filtering, error handling'
      contains: 'withRetry'
    - path: 'test/e2e/webfetch.e2e.ts'
      provides: 'E2E tests for WebFetch: real page fetch, error handling'
      contains: 'withRetry'
    - path: 'vitest.config.e2e.ts'
      provides: 'Separate vitest config for E2E tests with long timeouts'
      contains: 'test/e2e'
    - path: '.github/workflows/ci.yml'
      provides: 'PR gate CI workflow'
      contains: 'actions/setup-node'
  key_links:
    - from: 'test/e2e/websearch.e2e.ts'
      to: 'scripts/websearch.cjs'
      via: 'runScript spawn child_process'
      pattern: 'runScript.*websearch'
    - from: 'test/e2e/webfetch.e2e.ts'
      to: 'scripts/webfetch.cjs'
      via: 'runScript spawn child_process'
      pattern: 'runScript.*webfetch'
    - from: '.github/workflows/ci.yml'
      to: 'package.json'
      via: 'npm run lint/typecheck/test/build'
      pattern: 'npm run'
---

<objective>
Create end-to-end tests that validate real plugin behavior via bundled scripts with real network calls, and set up the GitHub Actions PR gate workflow.

Purpose: E2E tests catch bundle issues, stdin/stdout contract bugs, and real network failure modes that mocked unit tests cannot. The PR gate ensures every push and PR passes all quality checks before merge.
Output: Working E2E test suite and CI workflow that runs on every PR.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ci-pipeline-and-e2e-tests/06-CONTEXT.md
@.planning/phases/06-ci-pipeline-and-e2e-tests/06-RESEARCH.md
@.planning/phases/06-ci-pipeline-and-e2e-tests/06-PATTERNS.md
@.planning/phases/06-ci-pipeline-and-e2e-tests/06-01-SUMMARY.md

<interfaces>
<!-- Contracts from source code and prior plan that executors need. -->

From src/lib/input.ts (WebSearch stdin schema):

```typescript
export const WebSearchInputSchema = z.strictObject({
  query: z.string().min(2),
  allowed_domains: z.array(z.string()).optional(),
  blocked_domains: z.array(z.string()).optional(),
});
```

From src/lib/input.ts (WebFetch stdin schema):

```typescript
export const WebFetchInputSchema = z.strictObject({
  url: z.string(),
  prompt: z.string(),
});
```

From src/lib/output.ts (expected stdout format):

```typescript
// formatSearchResults returns XML:
// <search_results>
//   <result><title>...</title><url>...</url><snippet>...</snippet></result>
// </search_results>
```

From src/websearch.ts (success: stdout.write, error: stderr + exitCode=1):

```typescript
process.stdout.write(formatSearchResults(filtered));
// error path:
logger.error(err instanceof Error ? err.message : String(err));
process.exitCode = 1;
```

From src/webfetch.ts (success: stdout.write markdown, error: stderr + exitCode=1):

```typescript
process.stdout.write(markdown);
// error path:
logger.error(err instanceof Error ? err.message : String(err));
process.exitCode = 1;
```

From package.json (scripts added by Plan 01):

```json
"lint": "eslint . && prettier --check .",
"e2e": "npm run build && vitest run --config vitest.config.e2e.ts test/e2e/",
"check": "npm run lint && npm run typecheck && npm test -- --coverage && npm run build"
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create E2E test infrastructure and all E2E test files</name>
  <files>test/e2e/helpers.ts, test/e2e/websearch.e2e.ts, test/e2e/webfetch.e2e.ts, vitest.config.e2e.ts</files>
  <read_first>
    src/lib/input.ts (stdin schemas for WebSearch and WebFetch)
    src/lib/output.ts (formatSearchResults -- XML output structure)
    src/websearch.ts (entry point -- success/error paths)
    src/webfetch.ts (entry point -- success/error paths)
    test/helpers/mocks.ts (existing test helper pattern)
    .planning/phases/06-ci-pipeline-and-e2e-tests/06-RESEARCH.md (Pattern 2: E2E test patterns)
    .planning/phases/06-ci-pipeline-and-e2e-tests/06-PATTERNS.md (Pattern assignments for E2E files)
  </read_first>
  <behavior>
    - WebSearch E2E: runScript('scripts/websearch.cjs', {query: 'example domain'}) returns exitCode 0, stdout contains <search_results>, <result>, <title>, <url>, <snippet>
    - WebSearch E2E: runScript with {query: 'test', allowed_domains: ['example.com']} returns results filtered to example.com domain
    - WebSearch E2E: runScript with empty stdin or invalid JSON returns exitCode 1, stderr contains error
    - WebFetch E2E: runScript('scripts/webfetch.cjs', {url: 'https://example.com', prompt: 'describe'}) returns exitCode 0, stdout contains markdown (headings or paragraphs)
    - WebFetch E2E: runScript with {url: 'not-a-url', prompt: 'test'} returns exitCode 1, stderr contains error
    - All E2E tests use withRetry wrapper (maxRetries: 3) for transient network resilience
    - E2E tests run via `npm run e2e` using vitest.config.e2e.ts with testTimeout of 30000ms
  </behavior>
  <action>
  Per D-01 (real network calls), D-02 (invoke bundled scripts via stdin), D-03 (retry 2-3 times), D-04 (all 4 scenarios).

Step 1: Create vitest.config.e2e.ts as a separate vitest config for E2E tests. Include test/e2e/\*_/_.e2e.ts, set testTimeout to 30000 (30 seconds for real network calls), set hookTimeout to 30000. This keeps E2E test configuration independent from the unit test config.

Step 2: Create test/e2e/helpers.ts per RESEARCH.md Pattern 2 and PATTERNS.md assignment. Export two functions:

- `runScript(script: string, input: object): Promise<{stdout: string, stderr: string, exitCode: number | null}>` -- uses child_process.spawn to run `node [scriptPath]`, writes JSON to stdin, captures stdout and stderr. Resolve scriptPath relative to the test file location (import.meta.dirname + ../../ + script). Use `'pipe'` for all three stdio channels.
- `withRetry(fn: () => Promise<void>, maxRetries: number = 3): Promise<void>` -- wraps an async function, retries on failure up to maxRetries times, throws last error if all attempts fail. Per D-03.

Step 3: Create test/e2e/websearch.e2e.ts with three test cases per D-04:

1. "returns search results in XML format" -- runScript with {query: 'example domain website'}, wrapped in withRetry. Assert exitCode is 0, stdout contains `<search_results>`, `<result>`, `<title>`, `<url>`, `<snippet>`.
2. "filters results by allowed domains" -- runScript with {query: 'github', allowed_domains: ['github.com']}, wrapped in withRetry. Assert exitCode is 0, stdout contains `<search_results>`. All URLs in results should be from github.com domain -- extract URLs via regex matching `<url>(.*?)</url>` and assert each contains 'github.com'.
3. "returns error for invalid input" -- runScript with {} (empty object, no query field). Assert exitCode is 1, stderr is non-empty. No retry wrapper needed for deterministic error.

Step 4: Create test/e2e/webfetch.e2e.ts with two test cases per D-04:

1. "fetches real page and returns markdown" -- runScript with {url: 'https://example.com', prompt: 'describe this page'}, wrapped in withRetry. Assert exitCode is 0, stdout is non-empty. Validate markdown presence: stdout contains at least one of heading (#), paragraph text, or link pattern.
2. "returns error for bad URL" -- runScript with {url: 'not-a-valid-url', prompt: 'test'}. Assert exitCode is 1, stderr is non-empty. No retry wrapper.

Step 5: Run `npm run build` to ensure bundles exist, then run `npm run e2e` to verify all E2E tests pass. If DDG rate limiting causes failures, the retry wrapper should handle it. If persistent failures occur, adjust the query terms to less common ones.

NOTE on jsdom: Plan 01 fixes the jsdom bundle issue (external: ['jsdom']). If WebFetch E2E still fails with ENOENT, verify the build.ts change from Plan 01 was applied and bundles were rebuilt.
</action>
<verify>
<automated>npm run build && npm run e2e</automated>
</verify>
<done> - test/e2e/helpers.ts exports runScript and withRetry - test/e2e/websearch.e2e.ts has 3 tests: basic query, domain filtering, error handling - test/e2e/webfetch.e2e.ts has 2 tests: real page fetch, error handling - All 5 E2E tests pass with `npm run e2e` - E2E tests use withRetry for network-dependent tests - vitest.config.e2e.ts has 30s testTimeout for real network calls
</done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions PR gate workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    package.json (for script names)
    .planning/phases/06-ci-pipeline-and-e2e-tests/06-RESEARCH.md (Pattern 5: PR gate workflow)
    .planning/phases/06-ci-pipeline-and-e2e-tests/06-PATTERNS.md (Pattern assignment for ci.yml)
  </read_first>
  <action>
  Per D-09 (two workflows), D-10 (PR gate step sequence), D-11 (Node 20 only), D-16 (coverage in PR gate).

Create .github/workflows/ci.yml (create the .github/workflows/ directory first if it doesn't exist).

Workflow structure per RESEARCH.md Pattern 5:

- name: CI
- on: push to branches [master], pull_request to branches [master]
- concurrency: group = workflow name + ref, cancel-in-progress: true
- jobs.check runs-on: ubuntu-latest
- Steps in exact order per D-10:
  1. actions/checkout@v4
  2. actions/setup-node@v4 with node-version: 20 and cache: 'npm'
  3. npm ci
  4. npm run lint
  5. npm run typecheck
  6. npm test -- --coverage
  7. npm run build

No continue-on-error on any step. No E2E tests in PR gate (D-09: E2E is in cron only). No version matrix (D-11).

After creating the file, validate the YAML syntax by parsing it with Node.js: use `node -e` to read the file, parse it with a simple structural check (the project does not have a YAML parser installed, so validate key structural properties: file starts with `name:`, contains `on:` and `jobs:` keys at the root level, and has proper indentation of steps under jobs.check.steps).
</action>
<verify>
<automated>node -e "
const fs = require('fs');
const y = fs.readFileSync('.github/workflows/ci.yml', 'utf8');
const lines = y.split('\n');
// Structural YAML validation: check top-level keys exist
const hasName = lines.some(l => /^name:/.test(l));
const hasOn = lines.some(l => /^on:/.test(l));
const hasJobs = lines.some(l => /^jobs:/.test(l));
const hasSteps = lines.some(l => /^\s+- uses: actions\/checkout/.test(l));
const hasSetupNode = lines.some(l => /actions\/setup-node@v4/.test(l));
const hasNpmCi = lines.some(l => /npm ci/.test(l));
const hasCoverage = lines.some(l => /--coverage/.test(l));
const hasConcurrency = lines.some(l => /^concurrency:/.test(l));
console.assert(hasName, 'name key found');
console.assert(hasOn, 'on key found');
console.assert(hasJobs, 'jobs key found');
console.assert(hasSteps, 'checkout step found');
console.assert(hasSetupNode, 'setup-node@v4 found');
console.assert(hasNpmCi, 'npm ci found');
console.assert(hasCoverage, '--coverage found');
console.assert(hasConcurrency, 'concurrency found');
// Validate no tabs (YAML forbids tabs for indentation)
const hasTabs = lines.some(l => /^\t/.test(l));
console.assert(!hasTabs, 'no tab indentation');
console.log('ci.yml structure validated');
"</automated>
</verify>
<done> - .github/workflows/ci.yml exists with PR gate workflow - Triggers on push to master and pull_request to master - Runs: checkout, setup-node (v20, npm cache), npm ci, lint, typecheck, test with coverage, build - Concurrency group cancels redundant runs - No E2E tests in PR gate (E2E runs in cron only) - No continue-on-error -- CI fails on any step failure - YAML structure validated: correct top-level keys, proper step indentation, no tab characters
</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                  |
| -------------------------------------- | -------------------------------------------- |
| E2E test -> DDG (external)             | Real HTTP requests to DuckDuckGo Lite        |
| E2E test -> target websites (external) | Real HTTP requests to example.com and others |
| GitHub Actions -> npm registry         | CI installs packages from npm                |

## STRIDE Threat Register

| Threat ID | Category               | Component            | Disposition | Mitigation Plan                                                                                                   |
| --------- | ---------------------- | -------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------- |
| T-06-03   | Tampering              | DDG responses in E2E | accept      | E2E tests validate structure, not specific content. DDG results change over time. Retry handles transient issues. |
| T-06-04   | Information Disclosure | CI log output        | accept      | No secrets in E2E tests (DDG-only, no API keys). Coverage reports are non-sensitive.                              |

</threat_model>

<verification>
1. `npm run e2e` passes all 5 E2E tests
2. `.github/workflows/ci.yml` has all 7 steps in correct order
3. ci.yml uses actions/setup-node@v4 with node-version: 20 and npm cache
4. ci.yml has concurrency group with cancel-in-progress
5. E2E tests use withRetry for network-dependent scenarios
6. E2E error tests verify exitCode and stderr
7. ci.yml YAML structure validated (top-level keys, step indentation, no tabs)
</verification>

<success_criteria>

- 5 E2E tests pass: WebSearch (basic, domain filter, error), WebFetch (page fetch, error)
- GitHub Actions PR gate workflow configured and syntactically valid
- E2E tests test real bundled scripts via child_process.spawn with real network
- PR gate runs lint + typecheck + unit tests + coverage + build in sequence
  </success_criteria>

<output>
Create `.planning/phases/06-ci-pipeline-and-e2e-tests/06-02-SUMMARY.md` when done
</output>
