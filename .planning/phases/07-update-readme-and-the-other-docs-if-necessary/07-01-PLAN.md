---
phase: 07-update-readme-and-the-other-docs-if-necessary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .env.example
  - skills/webfetch/SKILL.md
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "User can read full README explaining what cc-websearch is and how to install it"
    - "User can see a config template showing all supported options with defaults and env var overrides"
    - "WebFetch skill definition references the correct compiled script path (.cjs not .js)"
    - "README includes comparison table, architecture section, output examples, and quick-start dev guide"
  artifacts:
    - path: README.md
      provides: "Complete project documentation for plugin users and developers"
      min_lines: 100
    - path: .env.example
      provides: "Config template for ~/.config/websearch/config.json with all options and env var equivalents"
      min_lines: 20
    - path: skills/webfetch/SKILL.md
      provides: "WebFetch skill definition with correct compiled script path"
      contains: "webfetch.cjs"
  key_links:
    - from: .env.example
      to: src/lib/config.ts
      via: "Zod schema fields, ENV_MAP keys, and DEFAULTS values"
      pattern: "retry|logging|WEBSEARCH_"
    - from: README.md
      to: package.json
      via: "build/test/typecheck/lint command names"
    - from: README.md
      to: src/lib/output.ts
      via: "formatSearchResults XML output format example"
    - from: skills/webfetch/SKILL.md
      to: scripts/webfetch.cjs
      via: "node script path reference"
      pattern: "webfetch\\.cjs"
---

<objective>
Produce all documentation assets for cc-websearch: rewrite README.md from one-liner to full project README, create .env.example config template, and fix the webfetch SKILL.md compiled-script extension bug.

Purpose: Users and developers need complete, accurate documentation to install, configure, use, and contribute to the plugin. The SKILL.md bug prevents the webfetch skill from working on `claude plugin install`.

Output:
- README.md (full rewrite)
- .env.example (new config template)
- skills/webfetch/SKILL.md (one-line fix)
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-update-readme-and-the-other-docs-if-necessary/07-CONTEXT.md
@.planning/phases/07-update-readme-and-the-other-docs-if-necessary/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From .claude-plugin/plugin.json:
```json
{
  "name": "cc-websearch",
  "displayName": "WebSearch",
  "version": "0.1.0",
  "description": "DDG-powered WebSearch and WebFetch replacement for Claude Code"
}
```

From src/lib/output.ts:
```typescript
export function formatSearchResults(results: SearchResult[]): string
// Produces <search_results> XML with <result>, <title>, <url>, <snippet> tags
```

From src/lib/config.ts Zod schema:
- retry: { maxRetries (0+ int, default 4), baseDelay (0+ int ms, default 1000), maxDelay (0+ int ms, default 16000), timeout (0+ int ms, default 30000) }
- logging: { level ("debug"|"info"|"warn"|"error", default "info") }
- ENV_MAP: WEBSEARCH_RETRY_MAX_RETRIES, WEBSEARCH_RETRY_BASE_DELAY, WEBSEARCH_RETRY_MAX_DELAY, WEBSEARCH_RETRY_TIMEOUT, WEBSEARCH_LOGGING_LEVEL
- Precedence: env var > config file > hardcoded defaults
- Config file path: ~/.config/websearch/config.json

From src/lib/input.ts:
- WebSearch schema: { query: string (min 2), allowed_domains?: string[], blocked_domains?: string[] }
- WebFetch schema: { url: string (valid URL), prompt: string (min 1) }

From build.ts:
- esbuild bundling: platform=node, format=cjs, outExtension={'.js':'.cjs'}, banner with #!/usr/bin/env node
- websearch.ts -> scripts/websearch.cjs (no externals)
- webfetch.ts -> scripts/webfetch.cjs (jsdom external)

Skills paths after fix:
- skills/websearch/SKILL.md: `${CLAUDE_PLUGIN_ROOT}/scripts/websearch.cjs` (already correct)
- skills/webfetch/SKILL.md: change from `webfetch.js` to `webfetch.cjs` (D-08 fix)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix webfetch SKILL.md compiled-script extension from .js to .cjs</name>
  <files>skills/webfetch/SKILL.md</files>
  <read_first>
    - skills/webfetch/SKILL.md (current state -- line 15 has the bug)
    - skills/websearch/SKILL.md (correct reference pattern for comparison)
    - build.ts (confirms esbuild outputs .cjs files)
  </read_first>
  <action>
    Read skills/webfetch/SKILL.md line 15. Change `webfetch.js` to `webfetch.cjs` in the bash example command that invokes the node script.

    The line currently reads:
    `echo '{"url":"URL","prompt":"QUESTION"}' | node "${CLAUDE_PLUGIN_ROOT}/scripts/webfetch.js"`

    Change it to:
    `echo '{"url":"URL","prompt":"QUESTION"}' | node "${CLAUDE_PLUGIN_ROOT}/scripts/webfetch.cjs"`

    This matches the actual compiled file produced by build.ts (esbuild outputs `scripts/webfetch.cjs`).
    Do not change anything else in the file. Do not change the websearch SKILL.md.

    Per D-08: fix webfetch SKILL.md -- change .js to .cjs to match actual compiled file.
    Per D-09: both skills use ${CLAUDE_PLUGIN_ROOT} for script paths -- verify the path variable is already correct.
  </action>
  <verify>
    <automated>grep -c 'webfetch\.cjs' skills/webfetch/SKILL.md && (grep -c 'webfetch\.js' skills/webfetch/SKILL.md | grep -q 0)</automated>
    <automated>npm test -- test/skills.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - webfetch SKILL.md line 15 references webfetch.cjs, not webfetch.js
    - No occurrences of webfetch.js remain in skills/webfetch/SKILL.md
    - Existing skills.test.ts still passes (the "should contain node and CLAUDE_PLUGIN_ROOT" test should pass; the compiled bundle tests should also pass)
  </acceptance_criteria>
  <done>WebFetch SKILL.md references correct compiled script path with .cjs extension. Existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create .env.example config template matching src/lib/config.ts Zod schema</name>
  <files>.env.example</files>
  <read_first>
    - src/lib/config.ts (Zod schema fields, ENV_MAP keys, DEFAULTS values, precedence logic)
    - .gitignore (confirm .env.example exclusion pattern -- already set)
  </read_first>
  <action>
    Create a new file at project root `.env.example` that documents the configuration options for the plugin.

    The file must contain two sections:

    **Section 1: Config file structure (`~/.config/websearch/config.json`)**
    Show a complete JSON example with every field from ConfigSchema in config.ts, including:
    - retry.maxRetries (integer, 0+, default: 4)
    - retry.baseDelay (integer ms, 0+, default: 1000)
    - retry.maxDelay (integer ms, 0+, default: 16000)
    - retry.timeout (integer ms, 0+, default: 30000)
    - logging.level (enum: debug/info/warn/error, default: info)

    Include inline comments describing each field's type, constraints, and default value.

    **Section 2: Environment variable overrides**
    Show the WEBSEARCH_* environment variables that override config file values:
    - WEBSEARCH_RETRY_MAX_RETRIES, WEBSEARCH_RETRY_BASE_DELAY, WEBSEARCH_RETRY_MAX_DELAY, WEBSEARCH_RETRY_TIMEOUT
    - WEBSEARCH_LOGGING_LEVEL

    Document the precedence: environment variable > config file > hardcoded default.

    Per D-15: create .env.example showing config template with all supported options and defaults.
    Do NOT include PPLX_API_KEY or any Perplexity-related fields -- DDG is the sole provider (Phase 5).
    Use placeholder values and explanatory comments only. No real API keys or secrets.
  </action>
  <verify>
    <automated>grep -c 'WEBSEARCH_RETRY_MAX_RETRIES' .env.example && grep -c 'WEBSEARCH_RETRY_BASE_DELAY' .env.example && grep -c 'WEBSEARCH_RETRY_MAX_DELAY' .env.example && grep -c 'WEBSEARCH_RETRY_TIMEOUT' .env.example && grep -c 'WEBSEARCH_LOGGING_LEVEL' .env.example && grep -c 'retry' .env.example && grep -c 'logging' .env.example && ! grep -c -i 'PPLX\|PERPLEXITY' .env.example</automated>
    <human-check>Compare .env.example against src/lib/config.ts: every ENV_MAP key, every ConfigSchema field, and every DEFAULTS value should have a matching entry. Verify no Perplexity/PPLX references exist.</human-check>
  </verify>
  <acceptance_criteria>
    - .env.example exists at project root
    - All 5 ConfigSchema fields from config.ts are present (retry.maxRetries, retry.baseDelay, retry.maxDelay, retry.timeout, logging.level)
    - All 5 ENV_MAP keys are present (WEBSEARCH_RETRY_MAX_RETRIES, etc.)
    - No Perplexity or PPLX references exist
    - .gitignore already has `!.env.example` (confirmed) so file will not be git-ignored
  </acceptance_criteria>
  <done>.env.example exists with all config options and env var overrides documented, matching config.ts exactly.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Write full README.md with all required sections</name>
  <files>README.md</files>
  <read_first>
    - README.md (current one-liner state)
    - .claude-plugin/plugin.json (plugin name, version, description)
    - skills/websearch/SKILL.md (skill description, input schema)
    - skills/webfetch/SKILL.md (skill description, input schema)
    - package.json (scripts: build, test, typecheck, lint, e2e, check)
    - build.ts (esbuild config for build section)
    - tsconfig.json (TypeScript target: ES2022, module: NodeNext)
    - src/lib/output.ts (formatSearchResults XML output format)
    - src/lib/input.ts (WebSearchInputSchema, WebFetchInputSchema)
    - src/lib/config.ts (config schema, env vars, precedence)
    - .gitignore (standard Node.js gitignore)
  </read_first>
  <action>
    Rewrite README.md from its current one-liner into a full project README with sections in this exact order:

    1. **Title + Description** (top heading)
       What cc-websearch is: DDG-powered drop-in replacement for Claude Code's WebSearch and WebFetch tools.
       No API keys required. Zero-config for basic use.

    2. **Quick Install**
       `claude plugin add <repo-url>` -- single command.
       Mention that compiled scripts ship in git (no build needed).

    3. **Usage**
       WebSearch: pipe `{"query": "..."}` JSON and get `<search_results>` XML.
       WebFetch: pipe `{"url": "...", "prompt": "..."}` JSON and get markdown.
       Show both invocations with bash command examples.
       Note that errors go to stderr, results to stdout.

    4. **Configuration**
       Config file at `~/.config/websearch/config.json`.
       Env var overrides (WEBSEARCH_RETRY_*, WEBSEARCH_LOGGING_LEVEL).
       Precedence: env > file > defaults.
       Default values for all options.
       Reference .env.example for a full template.

    5. **Feature Comparison** (per D-03)
       Table comparing plugin WebSearch vs built-in WebSearch for: query, allowed_domains, blocked_domains, output format, API key requirement.
       Table comparing plugin WebFetch vs built-in WebFetch for: url, prompt, content extraction, redirect handling, API key requirement.

    6. **Architecture** (per D-06)
       Explain the zero-API-key design: DDG is the sole search provider (scraped via duck-duck-scrape).
       WebFetch is a standalone content pipeline (fetch -> Readability extraction -> Turndown markdown).
       Citations extracted from DDG search result descriptions.
       No Perplexity dependency. No API keys needed for any functionality.

    7. **Output Examples** (per D-05)
       Show a real `<search_results>` XML snippet with title, url, and snippet tags.
       Show a markdown page content example from WebFetch.

    8. **Development** (per D-04)
       Prerequisites: Node.js 20 LTS+, npm.
       `npm install` (one time).
       `npm run build` (compile scripts with esbuild).
       `npm test` (run vitest unit tests).
       `npm run typecheck` (tsc --noEmit).
       `npm run lint` (ESLint + Prettier).
       `npm run check` (all of the above combined).
       Keep it to quick-start only. Do not add CHANGELOG, CONTRIBUTING, or SECURITY sections (per D-16).

    Writing rules:
    - Derive every claim from actual source files, not from ROADMAP/CONTEXT/PROJECT.md design docs
    - Never mention Perplexity, sonar models, or PPLX_API_KEY -- DDG is the sole provider since Phase 5
    - Use present tense, concrete examples
    - Installation instructions first (plugin users), dev section last
    - No emoji in prose
    - The section heading "Comparison" or "Feature Comparison" not "Feature Parity"
  </action>
  <verify>
    <human-check>
      1. Open rendered README.md in an editor/markdown viewer.
      2. Confirm all 8 sections listed above are present.
      3. Confirm Perplexity/sonar/PPLX references do NOT appear.
      4. Confirm no CHANGELOG, CONTRIBUTING, or SECURITY sections.
      5. Confirm comparison table matches the research data accurately.
      6. Confirm install command uses `claude plugin add` not npm.
    </human-check>
  </verify>
  <what-built>Complete README.md with installation, usage, configuration, comparison, architecture, output examples, and development quick-start sections.</what-built>
  <how-to-verify>
    1. Run `cat README.md` to see the full content
    2. Check that all 8 required sections are present: Title/Description, Quick Install, Usage, Configuration, Feature Comparison, Architecture, Output Examples, Development
    3. Run `grep -i perplexity README.md` -- should return no matches
    4. Run `grep -c "CHANGELOG\|CONTRIBUTING\|SECURITY" README.md` -- should return 0
    5. Verify install instructions use `claude plugin add` (the correct plugin installation method)
  </how-to-verify>
  <resume-signal>Type "approved" if README is correct, or describe what needs changing.</resume-signal>
  <acceptance_criteria>
    - All 8 sections present in the specified order
    - No Perplexity, sonar, or PPLX references
    - No CHANGELOG, CONTRIBUTING, or SECURITY sections
    - Install instructions use `claude plugin add` not npm
    - Config section references ~/.config/websearch/config.json
    - Feature comparison table includes both skills (WebSearch + WebFetch)
    - Architecture section explains DDG-only, zero-API-key design
    - Development section includes build/test/typecheck/lint/check commands
  </acceptance_criteria>
  <done>Full README.md with all required sections written, reviewed, and approved. No Perplexity references. Plugin-user sections first, dev section last.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README content | Project documentation consumed by users; no code execution |
| .env.example | Configuration template; contains placeholder values only |
| SKILL.md path | Script path reference consumed by Claude Code plugin system |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | README.md | mitigate | Content derived from actual source files, not design docs; checkpoint review catches drift |
| T-07-02 | Information Disclosure | .env.example | mitigate | Placeholder values only; no real API keys or secrets |
| T-07-03 | Tampering | skills/webfetch/SKILL.md | mitigate | Single path change verified by grep + existing test suite |
</threat_model>

<verification>
- After Task 1: `npm test -- test/skills.test.ts` passes
- After Task 2: grep config.ts fields against .env.example to verify matching
- After Task 3: human review of README content
- After all tasks: `npm test` full suite passes
</verification>

<success_criteria>
1. README.md rewritten with all 8 required sections, no Perplexity references
2. .env.example created matching config.ts schema exactly
3. skills/webfetch/SKILL.md references webfetch.cjs (not webfetch.js)
4. All existing tests pass after changes
5. Human review confirms README accuracy
</success_criteria>

<output>
Create `.planning/phases/07-update-readme-and-the-other-docs-if-necessary/07-01-SUMMARY.md` when done
</output>
