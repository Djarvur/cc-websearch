---
phase: 12-output-compatibility
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - test/e2e/output-compatibility.e2e.ts
autonomous: true
requirements:
  - OUTP-01
  - OUTP-02
user_setup: []

must_haves:
  truths:
    - "D-01: Built-in WebFetch returns bare markdown to stdout — plugin already matches via Readability+Turndown"
    - "D-02: No format changes needed for WebFetch output — verified by behavioral harness"
    - "D-03: Verification uses well-formedness + usability standard — e2e harness proves Claude consumes output"
    - "D-05: WebFetch output is readable markdown with ATX headings, fenced code blocks, GFM tables"
    - "D-06/D-07: E2E harness exists that sends 8 prompt patterns (4 search + 4 fetch) to Claude CLI and checks Claude's response for evidence of output consumption"
    - "D-07: For search prompts, Claude's response contains URLs — proving the XML search results were parsed and cited"
    - "D-07: For fetch prompts, Claude's response references page content — proving the markdown was consumed"
    - "D-08: Test prompts match Phase 11's 8 prompt patterns (identical wording)"
    - "D-09: No empirical comparison against live built-in tools — harness tests plugin output consumption only"
    - "D-13/D-14: Harness does not test error output paths — current error behavior is correct per earlier decisions"
  artifacts:
    - path: "test/e2e/output-compatibility.e2e.ts"
      provides: "Automated output compatibility e2e test suite"
      min_lines: 180
      key_elements:
        - "Spawns claude CLI with spawn() args array (same T-11-01 pattern as Phase 11)"
        - "Parses NDJSON stream-json output for assistant message content (text and tool_use)"
        - "4 search tests asserting Claude's response contains URLs (evidence of citation)"
        - "4 fetch tests asserting Claude's response references page content"
        - "Per-test timeout of 180000ms (matching Phase 11 harness pattern)"
        - "All prompts are hardcoded string literals (no external input)"
  key_links:
    - from: "test/e2e/output-compatibility.e2e.ts"
      to: "claude CLI"
      via: "spawn('claude', ['-p', prompt, '--output-format', 'stream-json', '--verbose', '--plugin-dir', pluginDir])"
    - from: "test/e2e/output-compatibility.e2e.ts"
      to: "test/e2e/redirect-reliability.e2e.ts"
      via: "Pattern: adapt runClaude() helper from Phase 11 harness, adding assistant text extraction"
    - from: "test/e2e/output-compatibility.e2e.ts"
      to: "vitest virtest.config.e2e.ts"
      via: "Vitest e2e runner discovers test/e2e/**/*.e2e.ts"
---

<objective>
Create and run an automated behavioral e2e test suite that empirically validates Claude can consume plugin output — proof that WebSearch XML and WebFetch markdown formats are readable by Claude.

Purpose: OUTP-01 and OUTP-02 require behavioral verification (D-06) — not just unit tests. The harness checks that Claude cites search results in its response (D-07 pass threshold) and references fetched page content. D-08 requires reusing Phase 11's 8 prompt patterns. No empirical comparison against built-in tools (D-09).

Output: New test file `test/e2e/output-compatibility.e2e.ts` and test run results documenting e2e behavioral verification.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.planning/phases/12-output-compatibility/12-CONTEXT.md
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.planning/phases/12-output-compatibility/12-DISCUSSION-LOG.md

<interfaces>
Phase 11 harness pattern (source of truth for spawn + NDJSON parsing):

From test/e2e/redirect-reliability.e2e.ts (excerpt — runClaude helper):
```
async function runClaude(prompt: string): Promise<{ toolNames: string[]; stderr: string }> {
  const child = spawn('claude', [
    '-p', prompt, '--output-format', 'stream-json', '--verbose', '--plugin-dir', pluginDir,
  ]);
  // ... stdout buffering, JSON.parse each line
  // tool_use events nested in assistant message.content[]
  if (event.type === 'assistant' && event.message?.content) {
    for (const content of event.message.content) {
      if (content.type === 'tool_use') {
        // extract skill name from content.input.skill
      }
    }
  }
}
```

Key NDJSON event shapes from Phase 11 empirical discovery:
- assistant message: `{"type":"assistant","message":{"content":[{"type":"text","text":"..."},{"type":"tool_use","name":"Skill","input":{"skill":"cc-websearch:websearch",...}},...]}}`
- text content: `{"type":"text","text":"..."}` within message.content array
- result: `{"type":"result","result":{"content":"...","tool_use_id":"..."}}`

Stream-json requires `--verbose` when combined with `-p` (per Phase 11 discovery).

From vitest.config.e2e.ts:
```
include: ['test/e2e/**/*.e2e.ts']
```
New file `test/e2e/output-compatibility.e2e.ts` is auto-discovered.

From Phase 11 test prompts (D-08: Phase 12 reuses exact same prompts):
- Search (4): "Search the web for: What is the capital of Australia?", "Search the web for: latest AI news 2026", "Search the web for: TypeScript release notes", "Search the web for: PostgreSQL vs MongoDB comparison"
- Fetch (4): "Fetch the content at https://example.com", "Read https://example.com and tell me what it contains", "Summarize the content at https://www.wikipedia.org", "Check the API documentation at https://jsonplaceholder.typicode.com"

From test/e2e/helpers.ts:
```
import { spawn } from 'node:child_process';
const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);
// pluginDir = resolve(__dirname, '../../.claude-plugin');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create output-compatibility e2e test file</name>
  <files>test/e2e/output-compatibility.e2e.ts</files>
  <read_first>
    @/Users/nil/DiskD/W/Djarvur/cc-websearch/test/e2e/redirect-reliability.e2e.ts
    Read for the runClaude() spawn + NDJSON parsing pattern. This is the template. Must adapt the parsing to extract assistant text content (not just toolNames), and change the assertions from "skill was invoked" to "output was consumed."

    @/Users/nil/DiskD/W/Djarvur/cc-websearch/test/e2e/helpers.ts
    Read for the spawn pattern with args array and path resolution.

    @/Users/nil/DiskD/W/Djarvur/cc-websearch/vitest.config.e2e.ts
    Read to confirm include pattern is `test/e2e/**/*.e2e.ts`.
  </read_first>
  <action>
    Create a new file at test/e2e/output-compatibility.e2e.ts with the following structure:

    IMPORTS:
    - `import { afterEach, describe, it, expect } from 'vitest'`
    - `import { spawn, type ChildProcess } from 'node:child_process'`
    - `import { dirname, resolve } from 'node:path'`
    - `import { fileURLToPath } from 'node:url'`

    Use `const __filename = fileURLToPath(import.meta.url)` and `const __dirname = dirname(__filename)` for ES module __dirname. Resolve pluginDir as `resolve(__dirname, '../../.claude-plugin')`.

    Use an `activeChildren` Set&lt;ChildProcess&gt; for cleanup in afterEach (same pattern as redirect-reliability.e2e.ts).

    HELPER FUNCTION — getAllAssistantText(stdout: string): string:
    - Split stdout by newlines
    - For each non-empty line, attempt JSON.parse (skip lines that fail with try/catch)
    - For each parsed event where event.type === 'assistant' and event.message?.content exists:
      - Iterate event.message.content
      - For each content entry where content.type === 'text': accumulate content.text into an array
    - Join all text fragments with newlines and return as a single string
    - This captures ALL assistant messages including the initial "I'll search..." and the final response

    HELPER FUNCTION — runClaude(prompt: string): Promise&lt;{ assistantText: string; stderr: string }&gt;:
    - Spawn claude CLI with args array: spawn('claude', ['-p', prompt, '--output-format', 'stream-json', '--verbose', '--plugin-dir', pluginDir])
    - Must use spawn with args array (NOT exec/shell string) — T-11-01 pattern from Phase 11
    - Buffer stdout and stderr via 'data' events
    - On 'close' event: call getAllAssistantText(stdout) to extract assistant text, return { assistantText, stderr }
    - On 'error' event (spawn failed): reject with error message including claude CLI hint
    - Add child process to activeChildren Set, remove on 'close'
    - Use settled flag to prevent double-resolve (same pattern as Phase 11 harness)

    TEST STRUCTURE:
    describe('Output Compatibility', ...) as top-level block.
    afterEach hook that kills all children in activeChildren and clears the Set.

    SEARCH TEST CASES (4):
    Each test:
    - Calls runClaude with the search prompt
    - Uses .toHaveLength(8) or longer timeout of 180000ms (third argument to it())
    - Asserts that assistantText contains at least one URL pattern:
      expect(result.assistantText).toMatch(/https?:\/\/[^\s)]+/)
      This proves Claude parsed the XML search results and cited URLs in its response (D-07 pass threshold).
    - D-08: exact prompt wording reused from Phase 11

    Prompts for search tests:
    1. "Search the web for: What is the capital of Australia?"
    2. "Search the web for: latest AI news 2026"
    3. "Search the web for: TypeScript release notes"
    4. "Search the web for: PostgreSQL vs MongoDB comparison"

    FETCH TEST CASES (4):
    Each test:
    - Calls runClaude with the URL-based prompt
    - Asserts assistantText contains page-identifying content from the fetched URL:
      1. "Fetch the content at https://example.com" → expect(result.assistantText).toMatch(/Example Domain/i)
      2. "Read https://example.com and tell me what it contains" → expect(result.assistantText).toMatch(/Example Domain/i)
      3. "Summarize the content at https://www.wikipedia.org" → expect(result.assistantText).toMatch(/Wikipedia/i)
      4. "Check the API documentation at https://jsonplaceholder.typicode.com" → expect(result.assistantText).toMatch(/JSON|placeholder|typicode|api/i)
    - D-08: exact prompt wording reused from Phase 11

    FETCH TEST VERIFICATION NOTE:
    The example.com and wikipedia.org pages have stable, recognizable content. jsonplaceholder.typicode.com is an API testing service — Claude may describe it as "JSONPlaceholder" or "typicode" or "fake API." The regex `/JSON|placeholder|typicode|api/i` covers all reasonable response phrasings.

    ERROR HANDLING:
    - Use the settled flag pattern from Phase 11 harness to prevent double-resolve from close+error events
    - 'error' event (spawn failure): reject with message including Claude Code CLI on PATH hint
    - 'close' event with non-zero code: reject with stderr diagnostics (same as Phase 11)

    TIMEOUT:
    - Each it() block gets third argument of 180000 (180 seconds), matching Phase 11 harness pattern

    SECURITY:
    - All prompts are hardcoded string literals — no external input
    - spawn() with args array — no shell injection vector

    STRUCTURAL NOTES:
    - Import vitest functions from 'vitest', not from a relative path
    - Type annotations: use `type ChildProcess` import from `node:child_process`
    - File path resolution must work when the test is run from the project root
  </action>
  <verify>
    <automated>
      # Verify file exists
      ls test/e2e/output-compatibility.e2e.ts > /dev/null 2>&1 && echo "OK: file exists" || echo "FAIL: file not found"

      # Verify spawn() usage (not exec or execSync)
      grep -c 'spawn(' test/e2e/output-compatibility.e2e.ts && echo "OK: uses spawn" || echo "FAIL: spawn not found"
      grep -c '\bexec(' test/e2e/output-compatibility.e2e.ts && echo "WARN: exec found" || echo "OK: no exec usage"

      # Verify 8 it() blocks
      grep -c "it(" test/e2e/output-compatibility.e2e.ts

      # Verify URL pattern check for search tests
      grep -c "https\?://" test/e2e/output-compatibility.e2e.ts

      # Verify fetch page assertions
      grep -c "Example Domain" test/e2e/output-compatibility.e2e.ts
      grep -c "Wikipedia" test/e2e/output-compatibility.e2e.ts

      # Vitest discovery check
      npx vitest run --config vitest.config.e2e.ts test/e2e/output-compatibility.e2e.ts --reporter=verbose 2>&1 | head -20
    </automated>
  </verify>
  <acceptance_criteria>
    1. File test/e2e/output-compatibility.e2e.ts exists and is non-empty
    2. Test file uses spawn() with args array (NOT exec/execSync/shell string) — grep for 'spawn(' and no '\bexec('
    3. Test file has exactly 8 it() blocks (4 search + 4 fetch) — grep -c "it(" returns 8
    4. Each it() block has third-argument timeout of 180000
    5. All prompts are hardcoded string literals (no external input, no dynamic prompt construction)
    6. NDJSON parsing handles malformed lines gracefully (try/catch on JSON.parse)
    7. Assistant text extraction correctly collects all message.content[].type === 'text' entries
    8. Search tests assert URL pattern in assistant response (proves citation per D-07)
    9. Fetch tests assert page-specific content in assistant response (proves markdown consumption)
  </acceptance_criteria>
  <done>Test file created at test/e2e/output-compatibility.e2e.ts with 8 test cases, per-test 180s timeout, URL citation assertion for search tests, and page-content assertion for fetch tests.</done>
</task>

<task type="auto">
  <name>Task 2: Run output compatibility e2e test suite and report results</name>
  <files>test/e2e/output-compatibility.e2e.ts</files>
  <read_first>
    No additional files needed. The task reads the test file created in Task 1 and runs it.
  </read_first>
  <action>
    Run the output compatibility e2e test suite:

    ```
    npx vitest run --config vitest.config.e2e.ts test/e2e/output-compatibility.e2e.ts --reporter=verbose
    ```

    This command:
    - Uses the e2e vitest config (includes test/e2e/**/*.e2e.ts)
    - Targets only the output-compatibility test file
    - Uses verbose reporter to show individual test case results
    - Applies 180s timeouts per test (set in the test file)

    Expected behavior:
    - Test suite spawns `claude` CLI 8 times with different prompts
    - Each invocation may take 30-120 seconds depending on model response time
    - For search tests: Claude retrieves search results and cites URLs in its response
    - For fetch tests: Claude fetches the page and references its content
    - D-07 pass threshold: Claude cites search results (proven by URL pattern in assistant text)

    If claude CLI is not found (spawn ENOENT error):
    - The error-handling in the test file should produce a clear failure message
    - Report the failure along with the need for Claude Code CLI to be installed and on PATH

    If individual tests fail:
    1. Report which specific test patterns failed
    2. Note whether failures are intermittent (vary between runs) or consistent (always the same pattern)
    3. For search test failures: check if assistantText actually contains URLs (maybe the regex is too narrow)
    4. For fetch test failures: check if the page content changed (example.com, wikipedia.org content)
    5. Do NOT modify the test file to lower the assertion bar — report findings for planning

    Expected pass threshold: 8/8 tests pass to confirm output format is consumable by Claude (D-07).
    Given Phase 11 achieved 8/8 on redirect reliability and this phase builds on the same pipeline, 8/8 is the expected outcome.
  </action>
  <verify>
    <automated>
      npx vitest run --config vitest.config.e2e.ts test/e2e/output-compatibility.e2e.ts --reporter=verbose
    </automated>
    <note>
      This test suite spawns the claude CLI and may take up to 15 minutes total (8 prompts x up to 120s each). The vitest timeout of 180s per test handles individual timeouts. Total suite timeout comes from the overall vitest runner.
    </note>
  </verify>
  <acceptance_criteria>
    1. All 8 vitest test cases complete (no crashes, no orphaned processes)
    2. The test suite produces clear PASS/FAIL status for each of the 8 test cases
    3. For PASS: output format is confirmed consumable by Claude (D-07 threshold met)
    4. For FAIL: specific prompt patterns and assertion failures are documented for remediation planning
    5. All child processes are cleaned up (no orphaned claude CLI processes)
    6. stderr diagnostics are available for any failures (buffered in test helper)
  </acceptance_criteria>
  <done>Test suite run completes with results documented. If 8/8 pass: output format confirmed consumable by Claude (OUTP-01, OUTP-02 verified). If any fail: patterns reported for remediation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → claude CLI | Subprocess execution — prompt text flows from hardcoded string literals to spawned process args |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Tampering | test/e2e/output-compatibility.e2e.ts (subprocess invocation) | mitigate | Use spawn() with args array (not exec with shell string). All prompt values are hardcoded string literals, not external inputs. Verified by grep for 'spawn(' and absence of 'exec(' in the test file. Same pattern as Phase 11 T-11-01 mitigation. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installations in this plan. Zero new dependencies. Test file uses only existing dependencies (vitest, Node.js built-in child_process). |
</threat_model>

<verification>
1. Test file exists at test/e2e/output-compatibility.e2e.ts with min 180 lines
2. Test file uses spawn() with args array (verified by grep — no exec/execSync/shell-string usage)
3. Test file has 8 it() blocks with 180000ms timeouts
4. Each search test asserts URL pattern `/https?:\/\/[^\s)]+/` in assistant response (D-07 citation check)
5. Each fetch test asserts page-specific content (Example Domain, Wikipedia, jsonplaceholder keywords)
6. All 8 test cases can be discovered by vitest (dry run succeeds)
7. Test suite exit code is 0 on successful run
</verification>

<success_criteria>
- Test harness file created, properly structured, using correct spawn + NDJSON parsing pattern
- Assistant text extraction correctly collects all message content text
- Search tests assert URL citations in Claude's response (D-07 pass threshold)
- Fetch tests assert page content references in Claude's response (OUTP-02 consumption proof)
- All 8 test cases pass — output format confirmed consumable by Claude
- No exec() usage in test file — spawn with args array is the only subprocess pattern
</success_criteria>

<output>
Create `.planning/phases/12-output-compatibility/12-02-SUMMARY.md` when done
</output>
