---
phase: 12-output-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/output.test.ts
  - .planning/phases/12-output-compatibility/12-CROSS-PROVIDER.md
autonomous: true
requirements:
  - OUTP-01
  - OUTP-03
  - OUTP-04
user_setup: []

must_haves:
  truths:
    - "D-04: WebSearch XML structure verified — <search_results>, <result>, <title>, <url>, <snippet> in correct order, entity escaping, no provider comment"
    - "D-10: Cross-provider verification via code review — no provider-specific code paths, documented in CROSS-PROVIDER.md"
    - "D-12: Hooks assumed compatible across all providers — confirmed in CROSS-PROVIDER.md as pure JSON config"
    - "D-15: Existing content.test.ts truncation tests are sufficient — confirmed passing"
    - "D-16: Plugin truncates at 100KB with [... content truncated ...] marker — confirmed in content.ts"
    - "D-17: No additional truncation tests added — D-15 acknowledged"
    - "OUTP-01: WebSearch XML output is well-formed — valid tag nesting, proper opening/closing, no entity-breaking"
    - "OUTP-03: Plugin has zero provider-specific code paths — no branching on provider, no provider-aware config"
    - "OUTP-03: Hooks are pure JSON configuration — no runtime provider detection, no provider-dependent logic"
    - "OUTP-04: 100KB truncation test passes — existing content.test.ts confirms MAX_CONTENT_SIZE behavior"
    - "All existing unit tests pass — no regressions from any Phase 12 changes"
  artifacts:
    - path: "test/output.test.ts"
      provides: "XML well-formedness test added to existing formatSearchResults test suite"
      min_lines: 80
    - path: ".planning/phases/12-output-compatibility/12-CROSS-PROVIDER.md"
      provides: "Written verification that plugin has zero provider-specific code"
      min_lines: 15
      key_elements:
        - "Confirms no provider-specific branching in src/*.ts"
        - "Confirms hooks.json is pure JSON with no provider-specific logic"
        - "Confirms SKILL.md files have no provider-specific instructions"
  key_links:
    - from: "src/lib/output.ts"
      to: "test/output.test.ts"
      via: "formatSearchResults() is the function under test"
    - from: "src/lib/content.ts"
      to: "src/lib/content.ts"
      via: "MAX_CONTENT_SIZE = 100_000 is the constant under verification"
---

<objective>
Verify WebSearch XML output is well-formed, confirm WebFetch truncation works, document cross-provider compatibility, and ensure no regressions.

Purpose: Three of four OUTP requirements (OUTP-01 structural, OUTP-03, OUTP-04) are purely verification of existing code. This plan adds a well-formedness assertion to the XML output test, confirms the truncation test passes, documents cross-provider review, and runs the full suite to confirm no regressions. No structural code changes to any src/ file.

Output: Enhanced output.test.ts with well-formedness check, cross-provider verification document, and confirmation that all unit tests pass.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.planning/phases/12-output-compatibility/12-CONTEXT.md
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.planning/phases/12-output-compatibility/12-DISCUSSION-LOG.md

<interfaces>
Existing test patterns and source contracts:

From test/output.test.ts (current state):
```
describe('formatSearchResults', () => {
  it('should return empty XML for empty results array', ...)
  it('should produce valid XML with result, title, url, snippet tags for single result', ...)
  it('should escape XML entities in title, URL, and snippet', ...)
  it('should produce correct multi-element output for multiple results', ...)
  it('should always include snippet tag even when snippet is undefined', ...)
  it('should include snippet tag with empty content when snippet is empty string', ...)
  it('should place snippet tag between url and result closing tag', ...)
  it('should NOT include provider comment', ...)
})
```

From src/lib/output.ts (function under test):
```
export function formatSearchResults(results: SearchResult[]): string {
  // Produces: <search_results>\n  <result>\n    <title>...</title>\n    <url>...</url>\n    <snippet>...</snippet>\n  </result>\n</search_results>
}
```

From src/lib/content.ts (truncation under D-15/D-16):
```
const MAX_CONTENT_SIZE = 100_000;
// ... slice + append marker: markdown.slice(0, MAX_CONTENT_SIZE) + '\n\n[... content truncated ...]'
```

From test/content.test.ts (existing truncation test):
```
it('should truncate content exceeding 100KB with marker (D-06)', ...)
it('should NOT truncate content under 100KB', ...)
```

Key source structure — line count verification:
- output.test.ts: currently 79 lines (D-04 well-formedness test will add ~25 lines)
- content.test.ts: currently 148 lines (no changes — existing truncation tests are sufficient per D-17)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add XML well-formedness test and create cross-provider verification document</name>
  <files>
    test/output.test.ts
    .planning/phases/12-output-compatibility/12-CROSS-PROVIDER.md
  </files>
  <read_first>
    @/Users/nil/DiskD/W/Djarvur/cc-websearch/test/output.test.ts
    Read before modifying. The current test file is 79 lines. New test must be added after the existing "should NOT include provider comment" test (as the last it() block in the describe).

    @/Users/nil/DiskD/W/Djarvur/cc-websearch/src/lib/output.ts
    Read to confirm XML structure. The function produces lines with fixed indentation: `<search_results>`, then 2-space-indented `<result>` with 4-space-indented `<title>`, `<url>`, `<snippet>`. No raw text between tags. No self-closing tags. No XML declaration.

    @/Users/nil/DiskD/W/Djarvur/cc-websearch/src/websearch.ts
    @/Users/nil/DiskD/W/Djarvur/cc-websearch/src/webfetch.ts
    @/Users/nil/DiskD/W/Djarvur/cc-websearch/src/lib/fetch.ts
    @/Users/nil/DiskD/W/Djarvur/cc-websearch/src/lib/input.ts
    Read these four src files for cross-provider code review. Confirm no provider-specific branching, no `if (provider === ...)`, no `process.env.PROVIDER` or similar.

    @/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude-plugin/hooks/hooks.json
    Read for cross-provider review. Confirm hooks are pure JSON with no runtime provider detection.

    @/Users/nil/DiskD/W/Djarvur/cc-websearch/skills/websearch/SKILL.md
    @/Users/nil/DiskD/W/Djarvur/cc-websearch/skills/webfetch/SKILL.md
    Read for cross-provider review. Confirm no provider-specific instructions.
  </read_first>
  <action>
    **Part A: Add well-formedness test to test/output.test.ts**

    After the last existing it() block (index 8, "should NOT include provider comment"), add a new it() block:

    Test name: `should produce well-formed XML without raw text between tags, missing closing tags, or entity-breaking`

    The test MUST:
    1. Create a multi-result input with realistic data including special characters:
       ```
       [
         { title: 'Item 1 & Co', url: 'https://example.com?a=1&b=2', snippet: 'test <snippet>' },
         { title: 'Item 2', url: 'https://example.org', snippet: '' },
       ]
       ```
    2. Call formatSearchResults() with this input
    3. Assert the output starts with '<search_results>' on its own line — check `result.startsWith('<search_results>\n')`
    4. Assert the output ends with '</search_results>' on its own line (possibly with trailing newline) — check `result.trimEnd().endsWith('</search_results>')`
    5. Count opening `<result>` tags and closing `</result>` tags — they must be equal (use match() with regex)
    6. Assert that no line starts with both text and a tag (no raw text between tags). Check: for each non-empty line, if it contains '<', it must be a complete tag (either opening or closing) — split by lines and verify each line either starts with '<' or is empty
    7. Assert no unescaped `&` appears outside CDATA — check that `result` does not contain `& &` (double space between ampersands) or similar broken escape patterns. Specifically: every `&` in the output must be followed by `amp;`, `lt;`, `gt;`, or `quot;` (using regex: `/&(?!amp;|lt;|gt;|quot;)/g` should not match)
    8. Assert that `<title>`, `<url>`, and `<snippet>` appear in that order within each `<result>` block — reuse the position comparison pattern from the existing "should place snippet tag between url and result closing tag" test

    **Part B: Create cross-provider verification document**

    Create file `.planning/phases/12-output-compatibility/12-CROSS-PROVIDER.md` with content:

    ```
    # Cross-Provider Verification (OUTP-03)
    
    **Verified by:** code review and documentation check
    **Date:** {current date}
    
    ## Code Review Summary
    
    Reviewed src files for provider-specific code paths. Results:
    
    | File | Provider-Specific Code Found | Notes |
    |------|------------------------------|-------|
    | src/websearch.ts | No | Pure DDG search pipeline. No provider branching. |
    | src/webfetch.ts | No | Pure fetch pipeline. No provider branching. |
    | src/lib/fetch.ts | No | HTTP fetch utility. No provider awareness. |
    | src/lib/input.ts | No | CLI input validation. No provider awareness. |
    | src/lib/output.ts | No | XML formatting utility. No provider awareness. |
    | src/lib/content.ts | No | Markdown extraction pipeline. No provider awareness. |
    
    ## Configuration Review
    
    - hooks.json: Pure JSON configuration with static denial reasons. No runtime provider detection, no environment variable checks. Inline echo command is shell-agnostic.
    - SKILL.md files: No provider-specific instructions. Both describe the same usage pattern for any Claude Code provider.
    - plugin.json: No provider-specific configuration.
    
    ## Conclusion
    
    Plugin has zero provider-specific code paths, zero provider-aware configuration, and zero provider-dependent behavior. Hooks are a Claude Code framework feature that applies identically across all providers (Anthropic, OpenAI-compatible, self-hosted). Per D-12, hooks are assumed compatible across all providers.
    
    **Status:** OUTP-03 VERIFIED
    ```

    **Part C: (no files changed)** After creating the cross-provider doc, run the full unit test suite:
    ```bash
    npm test
    ```
    Verify all tests pass. If truncation tests fail, investigate whether content.ts MAX_CONTENT_SIZE has changed.

    Note on test running: run 'npm test' not 'npx vitest run' to respect the project's test runner configuration. If 'npm test' is not available, consult package.json for the test script.
  </action>
  <verify>
    <automated>
      # Verify XML well-formedness test exists
      grep -c "produce well-formed XML" test/output.test.ts

      # Verify the & escaping assertion uses correct regex
      grep -c "(&amp;|&lt;|&gt;|&quot;)" test/output.test.ts || grep -c "&(?!amp" test/output.test.ts

      # Verify cross-provider doc exists
      ls .planning/phases/12-output-compatibility/12-CROSS-PROVIDER.md > /dev/null 2>&1

      # Run full test suite
      npm test 2>&1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    1. test/output.test.ts has a new it() block at the end titled with "produce well-formed XML"
    2. The well-formedness test checks: starts with &lt;search_results&gt;\n, ends with &lt;/search_results&gt;, opening==closing &lt;result&gt; tags, no unescaped &amp; outside entities
    3. npm test passes with zero failures (includes truncation tests from content.test.ts for OUTP-04)
    4. .planning/phases/12-output-compatibility/12-CROSS-PROVIDER.md exists and documents that all src/ files have zero provider-specific code
    5. CROSS-PROVIDER.md explicitly states "OUTP-03 VERIFIED" in the conclusion
  </acceptance_criteria>
  <done>XML well-formedness test added to output.test.ts, cross-provider verification document created, full test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test assertions → formatSearchResults | No new boundary — existing tests exercise the same function |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | test/output.test.ts (new assertions) | accept | New test assertions are read-only checks against function output. No execution of untrusted input, no subprocess invocation, no file writes. No new attack surface. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installations in this plan. Zero new dependencies. Uses only existing vitest framework. |
</threat_model>

<verification>
1. File test/output.test.ts has new well-formedness test (grep for "produce well-formed XML")
2. File .planning/phases/12-output-compatibility/12-CROSS-PROVIDER.md exists
3. CROSS-PROVIDER.md concludes "OUTP-03 VERIFIED"
4. npm test exit code is 0 — all tests pass including content.test.ts truncation tests for OUTP-04
</verification>

<success_criteria>
- WebSearch XML format verified well-formed (structural assertion added to existing test)
- Cross-provider verification documented with code review evidence
- 100KB truncation confirmed (existing tests pass)
- Full test suite passes with no regressions
</success_criteria>

<output>
Create `.planning/phases/12-output-compatibility/12-01-SUMMARY.md` when done
</output>
