---
phase: 11-redirect-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/hooks/hooks.json
  - skills/websearch/SKILL.md
  - skills/webfetch/SKILL.md
autonomous: true
requirements:
  - RDIR-01
  - RDIR-02
  - RDIR-03
  - RDIR-04
user_setup: []

must_haves:
  truths:
    - "D-01/D-02/D-03/D-04: Built-in WebSearch tool is denied with reason 'unavailable' referencing cc-websearch:websearch"
    - "D-01/D-02/D-03/D-04: Built-in WebFetch tool is denied with reason 'unavailable' referencing cc-websearch:webfetch"
    - "D-04: Inline echo hook preserved — no Node.js script upgrade (no dynamic denial reason)"
    - "D-05/D-06/D-07: WebSearch SKILL.md description begins with 'Replacement for built-in WebSearch — '"
    - "D-05/D-06/D-07: WebFetch SKILL.md description begins with 'Replacement for built-in WebFetch — '"
    - "D-05/D-06: WebSearch SKILL.md body opens with paragraph stating its replacement role"
    - "D-05/D-06: WebFetch SKILL.md body opens with paragraph stating its replacement role"
    - "D-07: allowed-tools Bash(node *) unchanged in both SKILL.md files"
    - "D-12: Dynamic denial reason not implemented — static inline echo only"
  artifacts:
    - path: ".claude-plugin/hooks/hooks.json"
      provides: "Updated denial reason strings per D-03"
      grep_exact: "permissionDecisionReason\":\"WebSearch tool is unavailable. Use cc-websearch:websearch Skill instead."
    - path: "skills/websearch/SKILL.md"
      provides: "Replacement-first description and body per D-05/D-06"
      grep_exact: "Replacement for built-in WebSearch"
    - path: "skills/webfetch/SKILL.md"
      provides: "Replacement-first description and body per D-05/D-06"
      grep_exact: "Replacement for built-in WebFetch"
  key_links:
    - from: ".claude-plugin/hooks/hooks.json"
      to: "PreToolUse hook entries"
      via: "permissionDecisionReason string values changed (structure unchanged)"
    - from: "skills/websearch/SKILL.md"
      to: "model skill selection context"
      via: "description field and opening body paragraph"
    - from: "skills/webfetch/SKILL.md"
      to: "model skill selection context"
      via: "description field and opening body paragraph"
---

<objective>
Update denial reason text in hooks.json and skill descriptions in both SKILL.md files so that Claude reliably invokes plugin skills after built-in tools are denied.

Purpose: Phase 10 established hook infrastructure with a minimal denial reason. Phase 11 improves the denial text (adding "unavailable" context per D-02/D-03) and restructures SKILL.md descriptions (adding "Replacement for built-in" prefix per D-05/D-06). These changes increase the likelihood that Claude follows the redirect to plugin skills.

Output: Modified hooks.json with new denial reason strings, modified websearch/SKILL.md and webfetch/SKILL.md with replacement-first descriptions and body content.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.planning/phases/11-redirect-reliability/11-CONTEXT.md
@/Users/nil/DiskD/W/Djarvur/cc-websearch/.planning/phases/11-redirect-reliability/11-PATTERNS.md

<interfaces>
Current hook entries structure from .claude-plugin/hooks/hooks.json:

- Two PreToolUse entries, one each for matcher "WebSearch" and matcher "WebFetch"
- Each uses type "command" with inline echo producing hookSpecificOutput format
- Current denial reason strings (what gets replaced):
  - WebSearch: "Use cc-websearch:websearch Skill tool instead."
  - WebFetch: "Use cc-websearch:webfetch Skill tool instead."
- Entire JSON structure (matchers, hook type, hookSpecificOutput envelope, echo command) remains unchanged — only the two permissionDecisionReason values are updated.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update WebSearch and WebFetch denial reason strings in hooks.json</name>
  <files>.claude-plugin/hooks/hooks.json</files>
  <read_first>
    @/Users/nil/DiskD/W/Djarvur/cc-websearch/.claude-plugin/hooks/hooks.json
    Read this file to see the exact current JSON structure and the two permissionDecisionReason strings that need replacement.
  </read_first>
  <action>
    In .claude-plugin/hooks/hooks.json, replace the two permissionDecisionReason string values:

    1. WebSearch entry (currently at line 9): Change from
       "Use cc-websearch:websearch Skill tool instead."
       to
       "WebSearch tool is unavailable. Use cc-websearch:websearch Skill instead."

    2. WebFetch entry (currently at line 18): Change from
       "Use cc-websearch:webfetch Skill tool instead."
       to
       "WebFetch tool is unavailable. Use cc-websearch:webfetch Skill instead."

    These are the ONLY two string values that change. Everything else remains identical:
    - Inline echo command pattern (single quotes wrapping JSON body, escaped double quotes inside)
    - hookSpecificOutput envelope structure
    - hookEventName, permissionDecision field values
    - matcher values ("WebSearch", "WebFetch")
    - type: "command" and the file's overall JSON structure
    - Both hook entries remain separate (one per tool, per Phase 10 D-04)

    Use Edit tool with targeted string replacement to avoid accidental structural changes.

    Decision reference: D-02 (existing short form is correct, add "unavailable" clause), D-03 (revised text), D-04 (inline echo stays, no Node.js upgrade).
  </action>
  <verify>
    <automated>
      # Verify WebSearch reason string is correct
      grep -c 'permissionDecisionReason":"WebSearch tool is unavailable. Use cc-websearch:websearch Skill instead."' .claude-plugin/hooks/hooks.json | grep -q 1 && echo "OK: WebSearch reason" || echo "FAIL: WebSearch reason"

      # Verify WebFetch reason string is correct
      grep -c 'permissionDecisionReason":"WebFetch tool is unavailable. Use cc-websearch:webfetch Skill instead."' .claude-plugin/hooks/hooks.json | grep -q 1 && echo "OK: WebFetch reason" || echo "FAIL: WebFetch reason"

      # Verify the echo command produces valid JSON
      echo '{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"deny","permissionDecisionReason":"WebSearch tool is unavailable. Use cc-websearch:websearch Skill instead."}}' | python3 -m json.tool > /dev/null 2>&1 && echo "OK: WebSearch JSON valid" || echo "FAIL: WebSearch JSON invalid"

      echo '{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"deny","permissionDecisionReason":"WebFetch tool is unavailable. Use cc-websearch:webfetch Skill instead."}}' | python3 -m json.tool > /dev/null 2>&1 && echo "OK: WebFetch JSON valid" || echo "FAIL: WebFetch JSON invalid"

      # Verify old strings are absent (should return 0)
      grep -c 'Use cc-websearch:websearch Skill tool instead' .claude-plugin/hooks/hooks.json && echo "FAIL: old string still present" || echo "OK: old string removed"
    </automated>
  </verify>
  <acceptance_criteria>
    1. .claude-plugin/hooks/hooks.json contains the exact string 'permissionDecisionReason":"WebSearch tool is unavailable. Use cc-websearch:websearch Skill instead."' (verified via grep)
    2. .claude-plugin/hooks/hooks.json contains the exact string 'permissionDecisionReason":"WebFetch tool is unavailable. Use cc-websearch:webfetch Skill instead."' (verified via grep)
    3. The old WebSearch string 'Use cc-websearch:websearch Skill tool instead.' is absent from the file
    4. The old WebFetch string 'Use cc-websearch:webfetch Skill tool instead.' is absent from the file
    5. Each new inline echo command, when run in a shell, produces valid JSON that passes python3 -m json.tool
    6. The file's structural elements (matchers, type, hook entries, echo quoting pattern) are unchanged
  </acceptance_criteria>
  <done>Both permissionDecisionReason strings updated to D-03 text; JSON validity confirmed; old strings absent.</done>
</task>

<task type="auto">
  <name>Task 2: Update websearch and webfetch SKILL.md descriptions and body content</name>
  <files>skills/websearch/SKILL.md, skills/webfetch/SKILL.md</files>
  <read_first>
    @/Users/nil/DiskD/W/Djarvur/cc-websearch/skills/websearch/SKILL.md
    @/Users/nil/DiskD/W/Djarvur/cc-websearch/skills/webfetch/SKILL.md
    Read both files to see the current description values and body structure. The PATTERNS.md at @/Users/nil/DiskD/W/Djarvur/cc-websearch/.planning/phases/11-redirect-reliability/11-PATTERNS.md has the exact replacement values in the "Modification pattern" sections.
  </read_first>
  <action>
    Apply the same two changes to both skills/websearch/SKILL.md and skills/webfetch/SKILL.md:

    Change 1 — Frontmatter `description` field (per D-05):
    Prefix the existing description value with "Replacement for built-in [ToolName] — " where [ToolName] is "WebSearch" (for websearch/SKILL.md) or "WebFetch" (for webfetch/SKILL.md). The existing description text after the prefix stays exactly as-is.

    For skills/websearch/SKILL.md:
    - Current description: "Search the web using DuckDuckGo. Use this skill when the user needs current information, web search results, or to find web pages about a topic."
    - New description: "Replacement for built-in WebSearch — Search the web using DuckDuckGo. Use this skill when the user needs current information, web search results, or to find web pages about a topic."

    For skills/webfetch/SKILL.md:
    - Current description: "Fetch and summarize web page content. Use this skill when the user provides a URL and asks about its content."
    - New description: "Replacement for built-in WebFetch — Fetch and summarize web page content. Use this skill when the user provides a URL and asks about its content."

    Change 2 — Body opening paragraph (per D-06):
    Insert a new paragraph immediately after the `---` frontmatter closing delimiter and before the `# [SkillName]` heading (or immediately before the `## Usage` section, whichever keeps the heading first). The exact wording:

    For skills/websearch/SKILL.md: "This skill replaces the built-in WebSearch tool when unavailable."
    For skills/webfetch/SKILL.md: "This skill replaces the built-in WebFetch tool when unavailable."

    Placement options (at Claude's discretion per CONTEXT.md discretion area):
    - Option A: After `---` frontmatter close, before `# WebSearch` / `# WebFetch` heading (makes replacement message the very first content)
    - Option B: After `# [SkillName]` heading, before `## Usage` (keeps heading as section marker, replacement message first paragraph under heading)

    The `allowed-tools: Bash(node *)` field, `## Usage` section, code blocks, schema lists, and trailing instruction paragraphs remain completely unchanged in both files.

    Decision reference: D-05 (prefix descriptions with replacement tag), D-06 (body first paragraph states replacement role), D-07 (allowed-tools unchanged).
  </action>
  <verify>
    <automated>
      # Verify websearch description prefix
      grep -c 'Replacement for built-in WebSearch' skills/websearch/SKILL.md | grep -q 1 && echo "OK: websearch description prefix" || echo "FAIL: websearch description prefix"

      # Verify webfetch description prefix
      grep -c 'Replacement for built-in WebFetch' skills/webfetch/SKILL.md | grep -q 1 && echo "OK: webfetch description prefix" || echo "FAIL: webfetch description prefix"

      # Verify websearch body has replacement paragraph
      grep -c 'This skill replaces the built-in WebSearch tool when unavailable' skills/websearch/SKILL.md | grep -q 1 && echo "OK: websearch body paragraph" || echo "FAIL: websearch body paragraph"

      # Verify webfetch body has replacement paragraph
      grep -c 'This skill replaces the built-in WebFetch tool when unavailable' skills/webfetch/SKILL.md | grep -q 1 && echo "OK: webfetch body paragraph" || echo "FAIL: webfetch body paragraph"

      # Verify allowed-tools unchanged
      grep -c 'allowed-tools: Bash(node *)' skills/websearch/SKILL.md | grep -q 1 && echo "OK: websearch allowed-tools" || echo "FAIL: websearch allowed-tools"
      grep -c 'allowed-tools: Bash(node *)' skills/webfetch/SKILL.md | grep -q 1 && echo "OK: webfetch allowed-tools" || echo "FAIL: webfetch allowed-tools"

      # Verify old description text still present (only prefix was added)
      grep -c 'Search the web using DuckDuckGo' skills/websearch/SKILL.md | grep -q 1 && echo "OK: websearch original description preserved" || echo "FAIL: websearch original description missing"
      grep -c 'Fetch and summarize web page content' skills/webfetch/SKILL.md | grep -q 1 && echo "OK: webfetch original description preserved" || echo "FAIL: webfetch original description missing"
    </automated>
  </verify>
  <acceptance_criteria>
    1. skills/websearch/SKILL.md description field begins with exactly 'Replacement for built-in WebSearch — ' followed by the original description text
    2. skills/webfetch/SKILL.md description field begins with exactly 'Replacement for built-in WebFetch — ' followed by the original description text
    3. skills/websearch/SKILL.md body contains the paragraph 'This skill replaces the built-in WebSearch tool when unavailable.'
    4. skills/webfetch/SKILL.md body contains the paragraph 'This skill replaces the built-in WebFetch tool when unavailable.'
    5. Both SKILL.md files still have 'allowed-tools: Bash(node *)' unchanged
    6. Both SKILL.md files still contain their original description text (only the prefix was added, nothing removed)
    7. Both SKILL.md files still contain their `## Usage` section, code blocks, schema lists, and trailing instructions unchanged
  </acceptance_criteria>
  <done>Both SKILL.md files updated with replacement-first descriptions and opening body paragraphs per D-05/D-06; original content preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 1 modifications | Configuration file edits (hooks.json, SKILL.md) — no runtime code changes, no executable code introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Tampering | .claude-plugin/hooks/hooks.json | mitigate | Targeted string replacement only — Edit tool replaces specific permissionDecisionReason values without touching surrounding JSON structure. Verified by grep assertion that old strings are gone and file structure is intact. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installations in this plan. Zero new dependencies. |
</threat_model>

<verification>
1. hooks.json denial reason strings match D-03 exactly for both WebSearch and WebFetch entries
2. Each inline echo command produces valid JSON when executed in a shell
3. websearch/SKILL.md description begins with "Replacement for built-in WebSearch — "
4. webfetch/SKILL.md description begins with "Replacement for built-in WebFetch — "
5. Both SKILL.md files have opening body paragraph stating replacement role
6. All original content (allowed-tools, ## Usage, code blocks, schema) preserved in both files
</verification>

<success_criteria>
- hooks.json has two `permissionDecisionReason` strings updated to D-03 text
- Old "Use cc-websearch:websearch Skill tool instead." strings are absent
- Both SKILL.md files have replacement-first descriptions
- Both SKILL.md files have opening body paragraph stating replacement role
- All automated grep assertions pass
</success_criteria>

<output>
Create `.planning/phases/11-redirect-reliability/11-01-SUMMARY.md` when done
</output>
